---
phase: 03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - skills/scout-detect/SKILL.md
  - skills/scout-detect/.gitkeep
  - skills/job-scout/references/file-contract.md
autonomous: true
requirements: [DET-06, STR-02, STR-04]
tags: [skill, scout-detect, file-contract, batch-detection, manual-lock]

must_haves:
  truths:
    - "User typing /scout-detect triggers a working skill that orchestrates batch detection over the top-30 connection-weighted companies in master_targets.csv"
    - "The skill resolves data_dir via state.py, validates via validate_data.py, and invokes scripts/ats/detect.py detect-batch with ONE Bash call (verbatim-runnable)"
    - "The skill surfaces results to the user (confirmed/borderline/not_found counts) and points them at ats_detection_review.csv if borderline matches were found"
    - "The skill explicitly documents that ats_provider=manual rows are NEVER overwritten — preserving the user's manual lock from STR-02"
    - "ats_detection_review.csv is added to skills/job-scout/references/file-contract.md as a persistent file with /scout-detect listed as owner"
    - "scout-detect/.gitkeep is removed (replaced by SKILL.md)"
  artifacts:
    - path: "skills/scout-detect/SKILL.md"
      provides: "Skill orchestrating batch ATS detection on top-N connection-weighted companies"
      min_lines: 90
      contains: "name: scout-detect"
    - path: "skills/job-scout/references/file-contract.md"
      provides: "ats_detection_review.csv row added to Persistent files table"
      contains: "ats_detection_review.csv"
  key_links:
    - from: "skills/scout-detect/SKILL.md"
      to: "scripts/ats/detect.py"
      via: "Bash invocation: python3 ${CLAUDE_PLUGIN_ROOT}/scripts/ats/detect.py detect-batch"
      pattern: "scripts/ats/detect\\.py detect-batch"
    - from: "skills/scout-detect/SKILL.md"
      to: "scripts/state.py"
      via: "data_dir resolution at Step 1"
      pattern: "scripts/state\\.py resolve"
    - from: "skills/scout-detect/SKILL.md"
      to: "scripts/validate_data.py"
      via: "data_dir validation at Step 1"
      pattern: "scripts/validate_data\\.py"
    - from: "skills/job-scout/references/file-contract.md"
      to: "<data_dir>/ats_detection_review.csv"
      via: "Persistent files table row"
      pattern: "ats_detection_review\\.csv"
---

<objective>
Create the new `/scout-detect` skill and register the new `ats_detection_review.csv` canonical path in `file-contract.md`. The skill orchestrates batch ATS detection on the top-30 connection-weighted companies in master_targets.csv via ONE Bash call to `scripts/ats/detect.py detect-batch` (built in Plan 03-01) and surfaces the JSON summary to the user.

Purpose: This is the user-facing entry point for Phase 3 detection. Without it, users have no way to populate `ats_provider`/`ats_board_url`/`ats_slug_confidence` for their target companies short of editing the CSV by hand.

Output:
- `skills/scout-detect/SKILL.md` — new skill (~90-130 lines) following the established `scout-setup`/`scout-run` SKILL.md format
- `skills/scout-detect/.gitkeep` — removed (replaced by SKILL.md)
- `skills/job-scout/references/file-contract.md` — one row added to the Persistent files table for ats_detection_review.csv
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-RESEARCH.md
@.planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-PATTERNS.md
@.planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@skills/scout-setup/SKILL.md
@skills/scout-run/SKILL.md
@skills/job-scout/references/file-contract.md
@scripts/ats/detect.py

<interfaces>
<!-- Plan 03-01 delivers detect.py; this plan invokes it. -->
<!-- Required CLI contract from Plan 03-01: -->

```bash
# Detect-batch CLI shape (from Plan 03-01 Task 1):
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/ats/detect.py detect-batch \
  "<data_dir>/master_targets.csv" \
  [--limit N] \
  [--force] \
  [--data-dir "<data_dir>"]

# LAST stdout line is JSON dict:
{
  "total": 30,
  "confirmed": 18,
  "borderline": 4,
  "not_found": 6,
  "error": 2,
  "skipped": 0,
  "wall_clock_seconds": 12.4,
  "companies": [...]
}

# Exit codes: 0 success, 1 misuse, 2 missing config.json
```

D-04 path: `<data_dir>/ats_detection_review.csv` (persistent, append-only, owned by /scout-detect via detect.py)

D-01 sentinel: `ats_provider="none"` for negative results; SKILL prose must not write or imply `none_detected`.

STR-02 manual-lock: `ats_provider="manual"` is NEVER overwritten. SKILL must document this explicitly.
</interfaces>

<locked_decisions>
D-04: ats_detection_review.csv is a NEW canonical path in file-contract.md. Owner: /scout-detect via detect.py (append-only). User fills in `action` column after review.

STR-02 (verbatim from REQUIREMENTS.md): "/scout-detect honors `manual` and never overwrites it." SKILL.md must encode this in prose.

STR-04 (verbatim from REQUIREMENTS.md): "/scout-detect is idempotent — re-running on the same CSV is a no-op unless --force is passed." SKILL.md must surface --force semantics to the user.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skills/scout-detect/SKILL.md and remove the .gitkeep</name>
  <files>skills/scout-detect/SKILL.md, skills/scout-detect/.gitkeep</files>
  <read_first>
    - skills/scout-setup/SKILL.md (analog: full file — frontmatter lines 1-6, pre-read block lines 10-15, AskUserQuestion pattern, Bash block pattern with python3 ${CLAUDE_PLUGIN_ROOT}/scripts/...)
    - skills/scout-run/SKILL.md (analog: lines 1-50 — Step 0/1 data_dir resolution + validation pattern; lines 110-157 — [ATS-PREVIEW] block as ONE-Bash-call analog with failure-mode docblock)
    - .planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-PATTERNS.md (lines 330-410 — frontmatter pattern, pre-read block, Step 1 resolve+validate, ONE-Bash-call detect-batch invocation, failure-mode block)
    - .planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-RESEARCH.md (Pattern 8 lines 446-473 — skill structure + step-level decisions)
    - scripts/ats/detect.py (after Plan 03-01 lands — confirm exact CLI args + JSON output schema)
    - skills/job-scout/references/file-contract.md (referenced by the skill's pre-read block)
  </read_first>
  <action>
    Create `skills/scout-detect/SKILL.md` with the structure below. Then remove `skills/scout-detect/.gitkeep` (it was a placeholder from Plan 03-01).

    **Frontmatter — copy verbatim** (PATTERNS.md lines 339-345):
    ```yaml
    ---
    name: scout-detect
    description: Detect ATS providers for top-connection companies and populate ats_provider + ats_board_url + ats_slug_confidence in master_targets.csv. Triggers when the user types `/scout-detect` or asks to "detect job boards", "find which ATS my target companies use", "populate ATS fields", "scan for ATS coverage".
    allowed-tools: Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, TodoWrite
    version: 0.4.0
    ---
    ```
    Note: version is 0.4.0 (Phase 6 CON-16 will normalize all skills in lockstep; we set it now to be consistent with the v0.4 milestone target — Phase 6 audit will confirm).

    **Body skeleton — write the following sections in order:**

    **Opener (3-4 sentences, no heading):**
    > Run a one-shot ATS provider detection sweep over the user's top-N connection-weighted companies in `master_targets.csv`. Populates `ats_provider`, `ats_board_url`, and `ats_slug_confidence` so subsequent `/scout-run` invocations can produce real ATS-sourced listings. Idempotent: re-running on the same CSV is a no-op unless `--force` is passed; rows with `ats_provider=manual` are NEVER overwritten regardless of `--force`.

    **Pre-read block** (PATTERNS.md lines 351-356):
    ```markdown
    Read these before starting:
    - `${CLAUDE_PLUGIN_ROOT}/skills/job-scout/SKILL.md` (core skill knowledge)
    - `${CLAUDE_PLUGIN_ROOT}/skills/job-scout/references/file-contract.md` (where every file lives — including the new `ats_detection_review.csv`)
    ```

    **Step 1: Resolve data_dir and validate** — copy from scout-run/SKILL.md lines 23-34 verbatim (data_dir resolution via state.py + validate_data.py; same exit-code handling).

    **Step 2: Parse user arguments**
    Use `AskUserQuestion` ONLY if the user invocation is ambiguous. Default behavior:
    - No arguments → batch detect top 30 connection-weighted companies (the default)
    - `--all` flag → detect ALL companies in master_targets.csv (no limit)
    - `--force` flag → re-detect rows that already have `ats_provider` populated AND were detected ≤30 days ago (does NOT override `ats_provider=manual` rows)
    - `--limit N` → custom top-N instead of default 30

    Document these flags in a markdown table in the SKILL.

    **Step 3: Invoke detect-batch — ONE Bash call** (PATTERNS.md lines 380-394):
    ```bash
    python3 ${CLAUDE_PLUGIN_ROOT}/scripts/ats/detect.py detect-batch \
      "<data_dir>/master_targets.csv" \
      --limit 30 \
      --data-dir "<data_dir>"
    ```
    Document the `--force` and `--all` variants explicitly:
    - `--force` adds `--force` to the command
    - `--all` REPLACES `--limit 30` with the row count of master_targets.csv (so the skill omits --limit entirely when --all is in effect)

    Capture stdout. The JSON summary contains `total`, `confirmed`, `borderline`, `not_found`, `error`, `skipped`, `wall_clock_seconds`, `companies[]`.

    Exit codes:
    - 0 → detection completed; parse JSON summary and proceed to Step 4
    - 1 → misuse (bad args); surface stderr to user and stop
    - 2 → missing config.json; tell user to run `/scout-setup` first and stop

    **Step 4: Surface results to the user**
    Build a concise human-readable summary from the JSON. Format:
    ```
    ATS detection complete in <wall_clock_seconds>s.

    - Confirmed: <confirmed> companies (ats_provider populated, ats_slug_confidence ≥ 0.85)
    - Borderline: <borderline> companies (review at <data_dir>/ats_detection_review.csv)
    - Not found: <not_found> companies (ats_provider="none" — no provider matched)
    - Errors: <error> companies (transient — try `/scout-detect --force` later)
    - Skipped: <skipped> companies (already detected within last 30 days; pass --force to re-detect)
    ```

    **Step 5: Borderline review (conditional — only if `borderline` count > 0)**
    Print:
    ```
    📋 <borderline> borderline matches need manual review.
    Open <data_dir>/ats_detection_review.csv and either:
      - Set the `action` column to "accept" and edit `master_targets.csv` to set ats_provider + ats_board_url manually
      - Set the `action` column to "skip" if it's a wrong-company false positive
    Borderline rows do NOT have ats_slug_confidence set — they need your judgement before the next /scout-run picks them up.
    ```

    **Step 6: Tell the user what changes**
    ```
    Your master_targets.csv now has ats_provider populated for the top-<N> companies.
    The next /scout-run will:
      - Run [ATS-PREVIEW] Pass 1 against every row where ats_provider="greenhouse" — producing real ATS listings tagged source=ats:greenhouse
      - Lazy-inline-detect any company in the daily slate that still has empty ats_provider (Step 2b)
    Companies with ats_provider="none" are NOT re-detected on subsequent runs unless you pass --force to /scout-detect.
    Companies with ats_provider="manual" are NEVER overwritten — your manual lock is honored.
    ```

    **Failure modes block** (mirror scout-run/SKILL.md lines 151-157 structure — PATTERNS.md lines 401-407):
    ```markdown
    **Failure modes detect.py already handles (no skill-side handling needed):**
    - `ats_provider=manual` rows → skipped silently regardless of `--force` (idempotency lock; STR-02).
    - All providers return NOT_FOUND → company gets `ats_provider="none"` in master_targets.csv; run continues.
    - HTTP 200 + 0 jobs → company gets `ats_provider="<provider>"` and `ats_board_url=<url>` BUT `ats_slug_confidence` stays empty AND a row is appended to `ats_detection_review.csv` with `note=zero_open_roles`. The company IS on the provider; just no current openings.
    - Network timeout / HTTP error → bucketed as ERROR in the JSON summary; ats_provider stays empty; company eligible for re-detection on next run.
    - rapidfuzz not installed → detect.py exits 1 with install hint; skill surfaces this to user.
    - `<data_dir>/config.json` missing → detect.py exits 2; skill prompts user to run /scout-setup first.
    ```

    **Idempotency note** (STR-04 — make it user-visible):
    ```markdown
    ## Idempotency

    Re-running `/scout-detect` is a no-op for rows where:
    - `ats_provider` is non-empty AND `last_ats_hit_date` is within the last 30 days (default freshness window)
    - `ats_provider` is `manual` (user-locked — NEVER auto-overwritten, even with `--force`)

    Pass `--force` to re-detect any non-manual row. Stale rows (`last_ats_hit_date` >30 days old) are re-detected automatically without `--force`.
    ```

    **Then remove the placeholder:**
    ```bash
    rm skills/scout-detect/.gitkeep
    ```

    Commit message: `feat(03-02): add /scout-detect skill — batch ATS detection orchestration (DET-06, STR-02, STR-04)`
  </action>
  <verify>
    <automated>test -f skills/scout-detect/SKILL.md && test ! -f skills/scout-detect/.gitkeep && grep -q "^name: scout-detect$" skills/scout-detect/SKILL.md && grep -q "^version: 0.4.0$" skills/scout-detect/SKILL.md && grep -q "scripts/ats/detect.py detect-batch" skills/scout-detect/SKILL.md && grep -q "manual" skills/scout-detect/SKILL.md && grep -q "ats_detection_review.csv" skills/scout-detect/SKILL.md && grep -q "force" skills/scout-detect/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `skills/scout-detect/SKILL.md` exists; line count ≥90 (`wc -l skills/scout-detect/SKILL.md` returns ≥90)
    - `skills/scout-detect/.gitkeep` is REMOVED (file does not exist)
    - Frontmatter has `name: scout-detect`, `version: 0.4.0`, `description:` mentions "Detect ATS providers", `allowed-tools:` includes `Read, Write, Edit, Bash, Glob, Grep, AskUserQuestion, TodoWrite`
    - `grep -c "scripts/ats/detect.py detect-batch" skills/scout-detect/SKILL.md` returns ≥1 (the ONE-Bash-call invocation)
    - `grep -c "scripts/state.py resolve" skills/scout-detect/SKILL.md` returns ≥1 (Step 1 data_dir resolution)
    - `grep -c "scripts/validate_data.py" skills/scout-detect/SKILL.md` returns ≥1 (Step 1 validation)
    - `grep -c "ats_detection_review.csv" skills/scout-detect/SKILL.md` returns ≥1 (Step 5 borderline review)
    - `grep -c "manual" skills/scout-detect/SKILL.md` returns ≥3 (manual lock prose appears in opener + Step 6 + failure modes block)
    - `grep -c -i "force" skills/scout-detect/SKILL.md` returns ≥3 (--force flag documented)
    - `grep -c "zero_open_roles" skills/scout-detect/SKILL.md` returns ≥1 (D-02 case documented in failure modes)
    - `grep -c '"none"\|=none\|=\\"none\\"' skills/scout-detect/SKILL.md` returns ≥1 (D-01 sentinel mentioned in user-facing prose)
    - `grep -c "none_detected" skills/scout-detect/SKILL.md` returns 0 (D-01 — wrong sentinel must NOT appear)
    - SKILL.md uses `${CLAUDE_PLUGIN_ROOT}` for all internal plugin paths (not hardcoded paths) — `grep -c "\\${CLAUDE_PLUGIN_ROOT}" skills/scout-detect/SKILL.md` returns ≥3
    - One git commit lands with message starting `feat(03-02):`
  </acceptance_criteria>
  <done>
    /scout-detect skill is invocable; idempotency + manual-lock + --force semantics documented; failure modes mirror scout-run pattern; .gitkeep replaced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ats_detection_review.csv row to file-contract.md Persistent files table</name>
  <files>skills/job-scout/references/file-contract.md</files>
  <read_first>
    - skills/job-scout/references/file-contract.md (existing Persistent files table — lines 26-38; the row insertion goes IMMEDIATELY AFTER the runs.jsonl row at line 36, BEFORE the closing blank line at line 37)
    - .planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-PATTERNS.md (lines 574-592 — exact row format and table position)
    - .planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-RESEARCH.md (Pitfall 7 lines 521-525 — why this MUST be added before any code writes the file)
  </read_first>
  <action>
    Add ONE new row to the Persistent files table in `skills/job-scout/references/file-contract.md`. The table currently spans lines 28-38. Insert the new row IMMEDIATELY AFTER the `Run telemetry log` row (the row that ends with `v0.4 SCH-01.`), as the FINAL row of the Persistent files table.

    Use the Edit tool. Find this exact existing block (3 lines for context):
    ```
    | Resume bank | `{data_dir}/Resumes/` | User-curated. Scout reads, never writes. |
    | Run telemetry log | `{data_dir}/runs.jsonl` | `/scout-run` (appends one JSON line per run; created empty by `validate_data.py:validate_runs_log` at first run startup). v0.4 SCH-01. |

    **Schema for `master_targets.csv` and `JobScout_Tracker.xlsx` lives in `scripts/schema.py`.** Never inline column lists. If a column is referenced by name in a prompt, it must match `MASTER_TARGETS_COLUMNS` or `TRACKER_COLUMNS` exactly.
    ```

    Replace with (one new table row inserted before the blank line, plus the schema paragraph unchanged):
    ```
    | Resume bank | `{data_dir}/Resumes/` | User-curated. Scout reads, never writes. |
    | Run telemetry log | `{data_dir}/runs.jsonl` | `/scout-run` (appends one JSON line per run; created empty by `validate_data.py:validate_runs_log` at first run startup). v0.4 SCH-01. |
    | ATS detection review | `{data_dir}/ats_detection_review.csv` | `/scout-detect` via `scripts/ats/detect.py` (append-only). Borderline matches (rapidfuzz score 70-84) and zero-open-role boards (HTTP 200 + 0 jobs) land here for manual review. User fills in the `action` column ("accept" or "skip"). v0.4 DET-05. |

    **Schema for `master_targets.csv` and `JobScout_Tracker.xlsx` lives in `scripts/schema.py`.** Never inline column lists. If a column is referenced by name in a prompt, it must match `MASTER_TARGETS_COLUMNS` or `TRACKER_COLUMNS` exactly.
    ```

    The new row uses the same column schema as the existing rows: `| File | Path | Owner |`. The `Owner` column also documents WHEN entries land (borderline + zero-open) — matching the `runs.jsonl` row's pattern of including a v0.4 requirement ID (DET-05).

    Do NOT touch any other section of file-contract.md. The Per-run output table (line 47+) and the Setup pointer table (lines 11-13) stay unchanged. The "Why this matters" closing section also unchanged.

    Commit message: `docs(03-02): register ats_detection_review.csv in file-contract.md (DET-05)`
  </action>
  <verify>
    <automated>grep -c "ats_detection_review.csv" skills/job-scout/references/file-contract.md && grep -c "DET-05" skills/job-scout/references/file-contract.md && grep -c "Persistent files" skills/job-scout/references/file-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ats_detection_review.csv" skills/job-scout/references/file-contract.md` returns exactly `1` (added once, not duplicated)
    - `grep -c "DET-05" skills/job-scout/references/file-contract.md` returns ≥1 (requirement ID present in the new row)
    - `grep -c "/scout-detect" skills/job-scout/references/file-contract.md` returns ≥1 (owner attribution present)
    - The new row sits between the `Run telemetry log` row and the `**Schema for` paragraph — verifiable: `grep -A2 "v0.4 SCH-01" skills/job-scout/references/file-contract.md` shows ats_detection_review.csv on the next line
    - File still has the closing "Why this matters" section unchanged: `grep -c "## Why this matters" skills/job-scout/references/file-contract.md` returns `1`
    - File still has the Per-run output section unchanged: `grep -c "ats_raw" skills/job-scout/references/file-contract.md` returns ≥1 (Phase 1 SCH-02 row preserved)
    - One git commit lands with message starting `docs(03-02):`
  </acceptance_criteria>
  <done>
    ats_detection_review.csv is documented as a canonical persistent path; "single source of truth" rule satisfied; downstream skill prompts can reference this path knowing it's authoritative.
  </done>
</task>

</tasks>

<threat_model>
Phase 3 surface: low — no new auth, no new PII paths. This plan adds only documentation (SKILL.md prompt) and a file-contract.md row.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | SKILL prompt could be malicious if sourced externally | accept | SKILL.md is part of the plugin distribution; user installs it; same trust boundary as existing /scout-setup and /scout-run skills |
| T-03-07 | Tampering | User edits ats_detection_review.csv (action="accept") could mislead future runs | accept | The skill explicitly tells the user to manually edit master_targets.csv after reviewing; ats_detection_review.csv is informational, not authoritative; future detect runs read master_targets.csv as source of truth |
</threat_model>

<verification>
**Per-task verification:**
```bash
# Task 1:
test -f skills/scout-detect/SKILL.md && \
  test ! -f skills/scout-detect/.gitkeep && \
  grep -q "^name: scout-detect$" skills/scout-detect/SKILL.md

# Task 2:
grep -c "ats_detection_review.csv" skills/job-scout/references/file-contract.md  # → 1
```

**Plan-level gate (run after both tasks):**
```bash
# Skill format invariants
grep -c "^name: scout-detect$" skills/scout-detect/SKILL.md     # → 1
grep -c "^version: 0.4.0$" skills/scout-detect/SKILL.md         # → 1
grep -c "scripts/ats/detect.py detect-batch" skills/scout-detect/SKILL.md  # → ≥1
grep -c "none_detected" skills/scout-detect/SKILL.md            # → 0 (D-01 — wrong sentinel forbidden)

# file-contract.md invariants
grep -c "ats_detection_review.csv" skills/job-scout/references/file-contract.md  # → 1 (no duplicates)

# Cross-file consistency: SKILL.md references the path, file-contract.md owns it
grep -l "ats_detection_review.csv" skills/scout-detect/SKILL.md skills/job-scout/references/file-contract.md
# Both files should be listed.
```

No pytest dependency for this plan — file-contract.md and SKILL.md are documentation. Plan 03-01's pytest gate must still pass after this plan lands (regression check):
```bash
~/.job-scout-venv/bin/python3 -m pytest tests/ -q
```
Expected: `22 passed` (no change — Plan 03-02 doesn't touch tests or scripts).
</verification>

<success_criteria>
1. `skills/scout-detect/SKILL.md` exists and is well-formed: frontmatter has name+description+allowed-tools+version; body has Step 1 (data_dir resolution + validate), Step 3 (ONE Bash call to detect-batch), Step 5 (borderline review prompt), Step 6 (next-steps), failure modes block, idempotency note.
2. `skills/scout-detect/.gitkeep` is removed.
3. SKILL.md surfaces `--force` semantics AND explicitly states `ats_provider=manual` is NEVER overwritten (STR-02 + STR-04 user-visible).
4. SKILL.md uses `${CLAUDE_PLUGIN_ROOT}` for all internal paths (project convention).
5. `skills/job-scout/references/file-contract.md` has exactly ONE row added for `ats_detection_review.csv` in the Persistent files table, with `/scout-detect` as owner and `DET-05` as the requirement ID.
6. No `none_detected` literal appears in either modified file (D-01 — only `none` allowed).
7. No regression in Plan 03-01 tests: `~/.job-scout-venv/bin/python3 -m pytest tests/ -q` still reports 22 passed.
8. Both tasks land as separate commits with `feat(03-02):` and `docs(03-02):` prefixes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-detection-scout-detect-skill-lazy-inline-detect-dead-doc-ref-cleanup/03-02-SUMMARY.md` documenting:
- SKILL.md final line count + frontmatter excerpt
- Confirmation that .gitkeep was removed
- file-contract.md diff (the new row, with surrounding context)
- Any auto-fixes (e.g., wording adjustments, table alignment) — be specific
- Note for Plan 03 (parallel wave): /scout-detect is now invocable as a slash command; users can populate ats_provider before running /scout-run
- Note for Phase 6 CON-16: /scout-detect skill version is set to 0.4.0 ahead of the milestone close; CON-16 audit will confirm all four skills (job-scout, scout-setup, scout-run, scout-detect) match
</output>
