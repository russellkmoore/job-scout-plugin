---
phase: 06
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - skills/scout-run/SKILL.md
  - scripts/ats/preview.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - CON-16
  - CON-21
user_setup: []

must_haves:
  truths:
    - "scout-run/SKILL.md Step 2 retains item 3 (LinkedIn keyword search) verbatim including f_C-disabled rationale + URL pattern (P3)"
    - "scout-run/SKILL.md Step 2 items 1 (Career page direct read) and 2 (ATS board Chrome navigation) are deleted; replacement prose directs reader to Step 2.5 (P1)"
    - "scout-run/SKILL.md contains zero [ATS-PREVIEW] brand markers (P2 — 9 occurrences eliminated)"
    - "scout-run/SKILL.md Step 5 has explicit prose telling the LLM to populate ab_tier_counts in stats.json passthrough (D-1, P4)"
    - "scout-run/SKILL.md Step 6 has a Run Summary block at the top of report.md — total/A/B/C counts, Pass-1 share, wall-clock, per-provider breakdown, top regression suspects (OUT-01, OUT-02)"
    - "scout-run/SKILL.md Step 7.5 reads new_rows.json tier field (NOT grep -c on report) and emits WARNING line if any of three checks fail (CON-21, P7)"
    - "scout-run/SKILL.md Step 9 prints Run Summary to stdout via runs_log.py milestone-bar (OUT-03)"
    - "scout-run/SKILL.md frontmatter version is 0.4.0 (was 0.3.3); description rewritten to ATS-first (CON-16)"
    - "scripts/ats/preview.py docstring has [ATS-PREVIEW] brand removed; DSP-03 architectural commentary preserved (P2)"
    - "Phase-wide grep gate after this plan: 4 SKILL.md files at version 0.4.0 in lockstep with plugin.json"
  artifacts:
    - path: "skills/scout-run/SKILL.md"
      provides: "Step 2 deletion + Step 2.5 banner cleanup + Step 5 ab_tier_counts + Step 6 Run Summary + Step 7.5 validation + Step 9 stdout mirror + version bump"
      contains: "ab_tier_counts"
    - path: "skills/scout-run/SKILL.md"
      provides: "Run Summary block at top of report (OUT-01 + OUT-02)"
      contains: "Run Summary"
    - path: "skills/scout-run/SKILL.md"
      provides: "Step 7.5 post-write validation block (CON-21)"
      contains: "post-run validation failed"
    - path: "scripts/ats/preview.py"
      provides: "DSP-03 architecture commentary preserved; [ATS-PREVIEW] brand removed"
      contains: "ONE process invocation per /scout-run"
  key_links:
    - from: "scout-run/SKILL.md Step 5"
      to: "runs_log.py append_run ab_tier_counts kwarg"
      via: "stats.json passthrough"
      pattern: "ab_tier_counts"
    - from: "scout-run/SKILL.md Step 6 Run Summary"
      to: "runs_log.py milestone-bar CLI"
      via: "Bash invocation"
      pattern: "runs_log.py milestone-bar"
    - from: "scout-run/SKILL.md Step 7.5 A-tier check"
      to: "new_rows.json tier field"
      via: "json.load + filter"
      pattern: "tier.*==.*A"
    - from: "scout-run/SKILL.md Step 9"
      to: "stdout (cron logs)"
      via: "milestone-bar JSON print"
      pattern: "milestone-bar"
---

<objective>
Surgically integrate Phase 6 changes into `skills/scout-run/SKILL.md` — the most-modified file in the v0.4 milestone. Touches the file ONCE in a single plan to avoid intra-phase merge conflicts: Step 2 deletion (P1, P3), Step 2.5 banner cleanup (P2), Step 5 ab_tier_counts write (D-1, P4), Step 6 Run Summary block (OUT-01, OUT-02), Step 7.5 post-write validation (CON-21, P7), Step 9 stdout mirror (OUT-03), frontmatter version bump (CON-16). Plus a small `scripts/ats/preview.py` docstring brand cleanup (P2).

Purpose: scout-run/SKILL.md is the convergence point for 6 of Phase 6's 12 requirements. Touching it once lets the executor audit all 7 changes against the file in a single review and prevents merge conflicts.

Output: Two files modified — `skills/scout-run/SKILL.md` (~7 surgical edits + 1 frontmatter bump) and `scripts/ats/preview.py` (docstring brand cleanup). After this plan, Plan 06-05's phase-wide grep gate has clean ground.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-VALIDATION.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-01-PLAN.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-02-PLAN.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-03-PLAN.md
@CLAUDE.md
@skills/scout-run/SKILL.md
@scripts/ats/preview.py
@scripts/ats/runs_log.py

<interfaces>
Step 2 — current state (lines 78–106): Items 1+2 (lines 87–89) DELETE. Item 3 (lines 90–97) PRESERVE.

Step 2.5 — current state (lines 149–223): Heading + migration prose + 9 [ATS-PREVIEW] markers. Surgery per Task 1.

Step 5 — current state (line 319+): After (b) sub-step, ADD (d) ab_tier_counts write.

Step 6 — current state (lines 387–495): ADD Run Summary block at top, BEFORE existing `### Header`.

Step 7 — current state (lines 499–509): After Step 7 tracker append, INSERT new top-level Step 7.5.

Step 9 — current state (lines 525–533): After existing chat summary, ADD stdout mirror sub-block.

Frontmatter (lines 1–6): version 0.3.3 → 0.4.0; description ATS-first rewrite.

preview.py docstring (lines 1–55): Lines 2, 54, 83, 95, 201 contain "[ATS-PREVIEW]" brand. DSP-03 commentary at lines 6–13 PRESERVE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 2 deletion + Step 2.5 banner cleanup + frontmatter bump (P1, P2, P3, CON-16)</name>
  <files>skills/scout-run/SKILL.md</files>

  <read_first>
    - skills/scout-run/SKILL.md (full file — verify current line numbers; user historically has pending edits at line 5 + lines 80–93; preserve any uncommitted hunks)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 237–290 — exact Step 2 + Step 2.5 surgery targets)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md (lines 215–268 — legacy deletion scope, [ATS-PREVIEW] cleanup scope)
  </read_first>

  <action>
**Edit 1 — Frontmatter (CON-16):**

Line 5: change `version: 0.3.3` to `version: 0.4.0`.

Line 3: replace the description from `Run a daily job search — broad sourcing across LinkedIn, career pages, and other boards, with honest scoring and actionable per-match output.` to:

`Run a daily job search — ATS-first sourcing (Greenhouse, Lever, Ashby, SmartRecruiters, Workday) plus JSON-LD fallback, LinkedIn keyword search, and specialized boards (Wellfound, Built In Seattle, YC Work at a Startup, HN Who is Hiring), with honest scoring and actionable per-match output.`

Preserve the trigger phrases at the end (`Triggers when the user types /scout-run...`).

**Edit 2 — Step 2 deletion (P1 + P3, OUT-04):**

Delete lines 87–89 (items 1+2). PRESERVE lines 90–97 (item 3) verbatim — including the URL pattern, encoding rules, f_C-disabled rationale, and "verified 2026-04-27 run" empirical evidence.

Replace lines 85–89 (the preamble + items 1+2) with:

```
For each selected company, ATS sourcing runs in **Step 2.5** (multi-provider, ONE process per run). The remaining company-side activity in this step is the LinkedIn keyword search:

3. **LinkedIn jobs — keyword scoped to company name + candidate's location.** Use the URL pattern below. **Do NOT use LinkedIn's `f_C=` company-ID filter — it is unreliable and routinely returns 0 results even when roles exist.**
```

The numbering "3." is preserved as a nod to the original 3-item list. If a Markdown linter complains, change to "1." at the executor's discretion (does not affect functionality).

**CRITICAL — Pitfall 3:** Lines 92–97 (URL pattern + encoding rules + budget rule + verified-evidence prose) MUST be preserved verbatim. Lines 99–106 (per-listing extraction block + budget rule) MUST be preserved verbatim. Only lines 87–89 (items 1+2 = career-page direct read + ATS board Chrome navigation) are deleted.

**Edit 3 — Step 2.5 banner cleanup (P2, OUT-04 — 9 [ATS-PREVIEW] occurrences):**

Per grep evidence (line numbers may shift after Edit 2), there are 9 occurrences in scout-run/SKILL.md at: 112, 142, 149, 151, 166, 193, 204, 206, 213. Each requires an exact replacement:

(a) Line 149 heading: change `## Step 2.5: [ATS-PREVIEW] Pass 1 (Greenhouse only) — additive` to `## Step 2.5: Pass 1 (ATS) — multi-provider`.

(b) Line 151 migration banner: replace the entire paragraph beginning `**What this is:** Phase 2 of the v0.4 ATS-first migration ships...` with:

```
**What this is:** ATS-first sourcing using the multi-provider dispatcher (Greenhouse, Lever, Ashby, SmartRecruiters, Workday, plus JSON-LD fallback). All providers run in ONE process per `/scout-run` with per-provider semaphore caps. Listings produced here flow into the same scoring + tier assignment as LinkedIn-sourced listings; cross-source dedup runs in Step 4.5 and the +1 ATS tier bump applies in Step 5.
```

(c) Line 112 (Step 2b prose): replace `[ATS-PREVIEW] (Step 2.5 below)` with `Step 2.5 (ATS Pass 1)`. Replace `Confirmed hits flip the company into Step 2.5's [ATS-PREVIEW] eligibility` with `Confirmed hits flip the company into Step 2.5's ATS Pass 1 eligibility`.

(d) Line 142 (Step 2b BORDERLINE): replace `so [ATS-PREVIEW] (Step 2.5) picks it up next run` with `so Step 2.5 (ATS Pass 1) picks it up next run`.

(e) Line 166 (no-mappable-companies print): replace `Print "[ATS-PREVIEW] No mappable companies in master_targets.csv (run /scout-detect to populate ats_provider columns)." to stdout` with `Print "No mappable companies in master_targets.csv (run /scout-detect to populate ats_provider columns)." to stdout`.

(f) Line 193: change `2. **Invoke the [ATS-PREVIEW] driver — ONE Bash call.**` to `2. **Invoke the ATS dispatcher driver — ONE Bash call.**`.

(g) Line 204: change `Capture stdout — the SKILL parses it to render the [ATS-PREVIEW] block in Step 6.` to `Capture stdout — the SKILL uses it to populate the Run Summary block in Step 6 and the per-listing render below.`.

(h) Lines 206–215 (render block + Phase-2-4-plumbing footer): replace the entire block beginning `3. **Render in the report.** In Step 6 below...` through the Phase 2-4 plumbing footer line with:

```
3. **Pass listings into the unified candidate set.** ATS listings produced by `preview.py` are scored and tier-assigned alongside LinkedIn listings. They no longer render in a separate `[ATS-PREVIEW]` section — they appear in the standard A-tier / B-tier / C-tier blocks in Step 6 with `**Source:** ats:<provider>`. Cross-source dedup (Step 4.5) reconciles overlap with LinkedIn listings; the +1 ATS tier bump (Step 5) applies to fresh ATS listings.
```

(i) Line 218 (failure modes first bullet): replace `the company silently does not contribute to [ATS-PREVIEW]` with `the company silently does not contribute to Pass 1`.

(j) Line 222 (failure modes last bullet): replace `skips Steps 3/4/5/6 of the [ATS-PREVIEW] block` with `skips the remaining sub-steps of Step 2.5`.

After all 10 edits (a-j), `grep -c '\\[ATS-PREVIEW\\]' skills/scout-run/SKILL.md` MUST return 0.

DELEGATE the Step 2.5 paragraph rewrite + render-block rewrite to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read current SKILL.md Step 2.5 section + 06-PATTERNS.md surgery targets.
2. Call `mcp__cf-code-assistant__generateCode` with description "Rewrite Step 2.5 banner from migration prose to v0.4-final ATS-first prose, preserving DSP-03 contract language" plus the read context.
3. Apply via Edit tool. Preserve byte-identical content elsewhere in the file.

If cf-code-assistant unavailable, write directly per the verbatim replacements above.
  </action>

  <verify>
    <automated>scripts/ats/_verify_06_04_task1.sh</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "\\[ATS-PREVIEW\\]" skills/scout-run/SKILL.md` returns 0 (P2).
    - `grep -c "^version: 0.4.0" skills/scout-run/SKILL.md` returns 1 (CON-16 — final lockstep).
    - `grep -c "Career page.*read directly" skills/scout-run/SKILL.md` returns 0 (P1 — item 1 deleted).
    - `grep -E "[12]\\. \\*\\*Career page\\*\\*|[12]\\. \\*\\*ATS board\\*\\*" skills/scout-run/SKILL.md` returns 0 lines (P1 — items 1+2 deleted).
    - `grep -c "f_C=" skills/scout-run/SKILL.md` returns at least 1 (P3 — LinkedIn rationale preserved).
    - `grep -c "linkedin.com/jobs/search/" skills/scout-run/SKILL.md` returns at least 1 (P3 — URL pattern preserved).
    - `grep -c "f_TPR=r604800" skills/scout-run/SKILL.md` returns at least 1 (P3 — past-week filter preserved).
    - `grep -c "verified 2026-04-27 run" skills/scout-run/SKILL.md` returns at least 1 (P3 — empirical evidence preserved).
    - `grep -c "ATS sourcing runs in \\*\\*Step 2.5\\*\\*" skills/scout-run/SKILL.md` returns at least 1 (P1 replacement prose).
    - `grep -c "ATS-first sourcing" skills/scout-run/SKILL.md` returns at least 1 (description rewrite).
    - `grep -c "broad sourcing across LinkedIn, career pages" skills/scout-run/SKILL.md` returns 0 (old description gone).
    - Step 2b (lazy inline detection) is intact except for the 2 prose substitutions in (c) + (d).
    - Step 3 (Pass 2) heading and content unchanged.
    - JSON-LD routing prose at lines 168–192 preserved verbatim (Phase 5 D-4 / Pitfall 4 content stays).

    To produce the `_verify_06_04_task1.sh` referenced in `<automated>` — Task 1's executor must create it at `scripts/ats/_verify_06_04_task1.sh` as a standalone bash script that performs all the grep checks above, exits 0 on all pass, exits 1 on any fail, and is committed alongside this task. After Plan 06-05 (the phase-wide gate) verifies grep gates, this helper script can be deleted, OR kept as a regression check.
  </acceptance_criteria>

  <done>
    Frontmatter at v0.4.0, description rewritten. Step 2 has only LinkedIn item 3; items 1+2 deleted with replacement prose. Zero [ATS-PREVIEW] markers in scout-run/SKILL.md. JSON-LD routing prose preserved. Step 2b 2 wording substitutions applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 5 ab_tier_counts + Step 6 Run Summary + Step 7.5 validation + Step 9 stdout mirror (D-1, P4, P7, OUT-01, OUT-02, OUT-03, CON-21)</name>
  <files>skills/scout-run/SKILL.md</files>

  <read_first>
    - skills/scout-run/SKILL.md (post-Task-1 state — confirm Step 5/6/7/9 line locations after Task 1 edits)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 292–396 — exact prose for Run Summary, Step 7.5, Step 9 stdout mirror)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md (lines 197–213 — Run Summary format; lines 350–379 — Step 7.5 pseudo-code)
    - scripts/ats/runs_log.py (post-Plan-06-02 — confirm milestone-bar CLI + ab_tier_counts kwarg landed)
  </read_first>

  <action>
Four independent insertions into post-Task-1 SKILL.md. Apply each as a separate Edit so diffs stay reviewable.

**Insertion A — Step 5 (d) ab_tier_counts write (D-1, P4):**

After Step 5 sub-step (c) "Assign final tier" (currently ends with the hard-cap rule near line 384), append a new sub-step (d) titled "Write ab_tier_counts for the milestone bar (D-1)":

The block instructs the LLM to count A and B tier listings by source after final tier assignment, build a dict with three keys (ats, linkedin, total_ab), and pass it to `runs_log.append_run(..., ab_tier_counts=...)` alongside the existing `dedup_decisions`, `regression_suspects`, `pass2_board_status` kwargs.

Source classification rules:
- `source` starts with `ats:` (e.g. `ats:greenhouse`, `ats:lever`, `ats:ashby`, `ats:smartrecruiters`, `ats:workday`, `ats:jsonld`) → counts toward `ats`.
- `source == "linkedin"` → counts toward `linkedin`.
- Pass-2 board sources (`builtin`, `wellfound`, `yc`, `hn`) → NOT included in this dict.
- C-tier listings → NOT included (milestone bar measures A/B share specifically per ROADMAP SC-5).

Add explicit Pitfall 6 callout: without this write, `runs_log.py milestone-bar` returns `pass1_share_pct: null` for this run, breaking the 5-run rolling average.

The full prose block to insert (verbatim from 06-PATTERNS.md lines 322–334 expanded):

```
(d) **Write `ab_tier_counts` for the milestone bar (D-1).**

After final tier assignment in (c), count A/B-tier listings by source and write the result into the stats.json passthrough that flows to the runs.jsonl append at end-of-run:

\`\`\`
"ab_tier_counts": {
  "ats": <count of A-tier and B-tier listings where source starts with "ats:">,
  "linkedin": <count of A-tier and B-tier listings where source == "linkedin">,
  "total_ab": <ats + linkedin>
}
\`\`\`

This dict is passed to `runs_log.append_run(..., ab_tier_counts=...)` alongside the existing `dedup_decisions`, `regression_suspects`, and `pass2_board_status` fields. Without this write, the `runs_log.py milestone-bar` subcommand returns `pass1_share_pct: null` for this run (Pitfall 6 — the field is OPTIONAL on older runs.jsonl lines but missing-on-new-runs is a regression).

C-tier listings are excluded per ROADMAP success criterion 5: "5-run rolling Pass-1 share ≥ 60% of A/B-tier candidates." C-tier doesn't count.

Source classification:
- `source` starts with `ats:` (e.g. `ats:greenhouse`, `ats:lever`, `ats:ashby`, `ats:smartrecruiters`, `ats:workday`, `ats:jsonld`) → `ats`.
- `source == "linkedin"` → `linkedin`.
- Pass-2 board sources (`builtin`, `wellfound`, `yc`, `hn`) are NOT in this dict — the milestone bar measures Pass-1 ATS share against LinkedIn-sourced candidates specifically.
```

**Insertion B — Step 6 Run Summary block at top of report (OUT-01, OUT-02):**

Insert a new sub-section AT THE TOP of Step 6, immediately after the `## Step 6: Build the daily report` heading and the `Write <data_dir>/daily/<TODAY>/JobScout_Report_<TODAY>.md. Structure:` line, BEFORE the existing `### Header` sub-section.

The block has three parts: (1) a Bash invocation of `runs_log.py milestone-bar` to fetch the rolling-average JSON, (2) a Markdown template for the Run Summary block to write into report.md, (3) a list of where each field comes from.

Specifically (verbatim from 06-PATTERNS.md lines 296–319):

```
### Run Summary block (top of report.md — OUT-02)

Insert this block at the very top of `report.md`, before the `### Header` and A-tier sections. Fields are computed from data already in scope (preview.py JSON output from Step 2.5; final scored candidate set from Step 5; one-time milestone-bar invocation):

\`\`\`bash
python3 ${CLAUDE_PLUGIN_ROOT}/scripts/ats/runs_log.py milestone-bar \\
  "<data_dir>/runs.jsonl" --lookback 5
\`\`\`

Capture the JSON output. Then write to `report.md`:

\`\`\`
## Run Summary — <TODAY>

- **Total listings:** <total scored A+B+C count>
- **A-tier:** <a> | **B-tier:** <b> | **C-tier:** <c>
- **Pass-1 (ATS) share:** <ats_ab>/<total_ab> A/B listings (<single_run_pct>% this run; 5-run rolling: <pass1_share_pct>% — target ≥ 60%)
- **Wall-clock (ATS fetch):** <wall_clock_seconds>s this run; 5-run avg <wall_clock_avg_seconds>s — target ≤ 300s. (ATS-fetch only, NOT total /scout-run wall-clock — D-2.)
- **Per-provider:** greenhouse=<ok>/<zero>/<err> lever=<ok>/<zero>/<err> ashby=<ok>/<zero>/<err> smartrecruiters=<ok>/<zero>/<err> workday=<ok>/<zero>/<err> jsonld=<ok>/<zero>/<err>
- **Top ATS regression suspects:** <up to 3 names from regression-suspects, or "none">
- **Milestone bar met:** <bar_met true/false>
\`\`\`

Sources of each field:
1. Total + A/B/C counts: from final scored candidate set (Step 5).
2. Pass-1 share + 5-run rolling: from `pass1_share_pct` field in milestone-bar JSON.
3. Wall-clock this run: from `wall_clock_seconds` in preview.py stdout JSON. Wall-clock 5-run avg: from `wall_clock_avg_seconds` in milestone-bar JSON.
4. Per-provider: from `per_provider_outcomes` in preview.py stdout JSON.
5. Regression suspects: from `runs_log.py regression-suspects` (already called below in Step 6 — capture top 3 by `prior_ok_count` desc).
6. Milestone bar met: `bar_met` field from milestone-bar JSON.

This block REPLACES the per-listing render that was in the deleted Step 2.5 [ATS-PREVIEW] section (Task 1 edit (h)). ATS listings now render in standard A/B/C tier blocks below with `**Source:** ats:<provider>`.
```

**Insertion C — Step 7.5 post-write validation (CON-21, P7):**

Insert a new top-level Step 7.5 section BETWEEN Step 7 (Update the tracker, ending around line 510) and Step 8 (Update master_targets.csv). The block is non-blocking (warnings only, never aborts the run).

Three checks in order: (1) report exists and is non-empty (using `test -s`); (2) runs.jsonl was appended this run (last line's timestamp contains `<TODAY>`); (3) A-tier count matches between report and tracker — using new_rows.json's `tier` field (NOT `grep -c '^### '` which would also match Honest notes / Stale / other level-3 headers — Pitfall 7).

For check (3), the explicit Python one-liner uses regex to find the `### A-tier` section in report.md, count level-3 headers within only that section (excluding the heading itself), and compare against `sum(1 for r in new_rows.json if r.get('tier')=='A')`.

Each check, on failure, prints a single-line `WARNING: post-run validation failed: <reason>` to stdout. After all three complete, prints `post-run validation: <N>/3 checks passed` summary.

The full prose block to insert is in 06-PATTERNS.md lines 364–396 — copy verbatim with `<data_dir>` and `<TODAY>` as the SKILL substitution placeholders.

**Insertion D — Step 9 stdout mirror (OUT-03):**

After existing Step 9 chat summary (ends near line 533), append a new sub-section "Stdout summary mirror (OUT-03)" BEFORE the `## On-demand: generate A-tier packet` heading.

The block invokes `runs_log.py milestone-bar` once more, prints the JSON output, and then prints a human-readable text version of the same Run Summary fields as the Step 6 block. This stdout dump is the auditing surface for cron/launchd-scheduled runs — the user does not need to open report.md.

Verbatim from 06-PATTERNS.md lines 343–356.

DELEGATE the Step 7.5 prose + Step 6 Run Summary block to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read post-Task-1 SKILL.md + 06-PATTERNS.md sections for these two large insertions.
2. Call `mcp__cf-code-assistant__generateCode` with description "Write Step 7.5 post-run validation prose + Step 6 Run Summary block for Markdown SKILL file" + the read context.
3. Apply via Edit tool — add insertions in order A → B → C → D.

If cf-code-assistant unavailable, write directly per the verbatim blocks above.

CRITICAL — preserve all existing Step 6 content (Header, A-tier render, B-tier render, C-tier table, Stale, Companies-checked, Honest notes, ATS regression suspects, Pass-2 board-broken warnings, Dedup decisions, Generate-on-demand line). Insertion B goes at the TOP of Step 6, before `### Header`.

CRITICAL — preserve all existing Step 9 content (chat summary). Insertion D appends; does not replace.
  </action>

  <verify>
    <automated>scripts/ats/_verify_06_04_task2.sh</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ab_tier_counts" skills/scout-run/SKILL.md` returns at least 2 (Insertion A defines + uses the field name).
    - `grep -c "Run Summary" skills/scout-run/SKILL.md` returns at least 1 (Insertion B header).
    - `grep -c "## Step 7.5" skills/scout-run/SKILL.md` returns exactly 1 (Insertion C — new top-level step).
    - `grep -c "post-run validation failed" skills/scout-run/SKILL.md` returns at least 1 (Insertion C — warning prefix).
    - `grep -c "milestone-bar" skills/scout-run/SKILL.md` returns at least 2 (Insertions B + D both invoke the CLI).
    - `grep -c "tier.*==.*'A'" skills/scout-run/SKILL.md` returns at least 1 (Insertion C — P7 — A-tier check uses tier field, not grep on report).
    - `grep -c "stdout summary mirror\\|Stdout summary mirror" skills/scout-run/SKILL.md` returns at least 1 (Insertion D heading).
    - `grep -c "ATS-fetch only" skills/scout-run/SKILL.md` returns at least 1 (D-2 documented in Run Summary).
    - `grep -c "C-tier listings are excluded" skills/scout-run/SKILL.md` returns at least 1 (D-1 — A/B-only scope explicit).
    - Step 6 still has `### Header`, `### A-tier`, `### B-tier`, `### C-tier`, `### Honest notes`, `### ATS regression suspects`, `### Pass-2 board-broken warnings`, `### Dedup decisions`, `### Generate-on-demand packets` sub-sections — all preserved (verify each via `grep -c "### Header" skills/scout-run/SKILL.md` returns 1, etc.).
    - Step 9 still has the chat summary (verify `grep -c "Counts: companies checked" skills/scout-run/SKILL.md` returns at least 1) — Insertion D appends, does not replace.
    - Per-provider list in Run Summary block names all 6 providers (greenhouse, lever, ashby, smartrecruiters, workday, jsonld).

    To produce the `_verify_06_04_task2.sh` referenced in `<automated>` — Task 2's executor must create it at `scripts/ats/_verify_06_04_task2.sh` as a standalone bash script with all grep checks above (exits 0 on pass, 1 on fail).
  </acceptance_criteria>

  <done>
    Step 5 (d) ab_tier_counts write block present. Step 6 has Run Summary block at top before `### Header`. Step 7.5 post-write validation block present between Step 7 and Step 8 with three non-blocking checks (P7-safe A-tier count using tier field). Step 9 stdout mirror appended before On-demand packet section. All preserved existing content intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/ats/preview.py docstring brand cleanup (P2)</name>
  <files>scripts/ats/preview.py</files>

  <read_first>
    - scripts/ats/preview.py (full file — confirm docstring structure at lines 1–55, in-function docstring at lines 83–95, --help text at line 201)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md (lines 258–268 — preview.py docstring scope)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 644–648 — Pitfall 2 — preserve DSP-03 commentary)
  </read_first>

  <action>
EDIT `scripts/ats/preview.py` to remove `[ATS-PREVIEW]` brand markers from user-facing prose while PRESERVING the architectural commentary (DSP-03 contract — ONE Client per run rationale at lines 6–13).

Five specific edits:

(a) Line 2 (module docstring opener): change `preview.py — Phase 2 [ATS-PREVIEW] driver. ONE process invocation per /scout-run.` to `preview.py — ATS dispatcher driver. ONE process invocation per /scout-run.`. The "ONE process invocation per /scout-run" architectural promise is PRESERVED.

(b) Line 54 (closing prose at end of module docstring): change `Phase 5 will fold this back into a single end-of-/scout-run append once Pass 2 dedup lands; Phase 2's per-block append is acceptable because the [ATS-PREVIEW] flow is the only ATS path in this milestone.` to `Per-block append is the v0.4 contract; rotation/aggregation is OOS for v0.4 per project convention.`.

(c) Line 83 (run_preview docstring): change `"""Run a single Phase 4 [ATS-PREVIEW] cycle and return the summary dict.` to `"""Run a single ATS Pass 1 cycle and return the summary dict.`.

(d) Line 95 (run_preview docstring continuation): change `to render the [ATS-PREVIEW] block in Step 6).` to `to populate the Run Summary block in Step 6).`.

(e) Line 201 (--help text inside `if __name__ == "__main__":`): change `"Run a single [ATS-PREVIEW] cycle for /scout-run Step 2.5.\n"` to `"Run a single ATS Pass 1 cycle for /scout-run Step 2.5.\n"`.

CRITICAL — preserve ALL of:
- Lines 6–13 (DSP-03 contract: "exactly ONE shared httpx.Client per /scout-run", three-call problem, three-Client violation, time measurement) — these are the architectural rationale, not brand.
- Line 14 (`Solution: this driver collapses all three responsibilities into ONE process`) — architectural commentary.
- Lines 30–48 (Behavior: numbered steps describing the in-order behavior) — architectural commentary.
- All function signatures and bodies — this task is docstring-only.

After these edits, `grep -c '\\[ATS-PREVIEW\\]' scripts/ats/preview.py` MUST return 0. The preview.py module continues to function identically — all changes are in docstring/help-text strings.

DELEGATE the docstring edits to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read `scripts/ats/preview.py` first to identify exact byte positions of the 5 brand markers.
2. Call `mcp__cf-code-assistant__generateCode` with description "Strip [ATS-PREVIEW] brand markers from preview.py docstrings, preserving DSP-03 architectural commentary" + the read context.
3. Apply via Edit tool. Verify Python syntax via `python3 -c "import ast; ast.parse(open('scripts/ats/preview.py').read())"` after editing.

If cf-code-assistant unavailable, write directly per the 5 specific edits above.
  </action>

  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/ats/preview.py').read()); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('scripts/ats/preview.py').read())"` exits 0 (Python syntax valid after edits).
    - `grep -c '\\[ATS-PREVIEW\\]' scripts/ats/preview.py` returns 0 (P2 — all 5 brand markers removed).
    - `grep -c "ONE process invocation per /scout-run" scripts/ats/preview.py` returns at least 1 (DSP-03 architectural promise preserved).
    - `grep -c "exactly ONE shared httpx.Client" scripts/ats/preview.py` returns at least 1 (DSP-03 contract preserved at line 6).
    - `grep -c "fetch_all" scripts/ats/preview.py` is unchanged from pre-edit count (function calls + commentary intact).
    - `python3 scripts/ats/preview.py --help` exits 0 and the help text mentions `ATS Pass 1` (not `[ATS-PREVIEW]`).
    - `python3 scripts/ats/preview.py --version` exits 0 and prints a version string.
    - Run Plan 02-01's smoke test (no-op fetch_all): `python3 scripts/ats/preview.py "/tmp" "2026-04-29" ""` against a temp dir with a minimal config.json — preview.py should still execute correctly (or fail with the expected `FileNotFoundError` if config.json missing, since this is a unit-level smoke check).
    - All existing pytest tests still pass: `~/.job-scout-venv/bin/python3 -m pytest tests/ -q` exits 0.
  </acceptance_criteria>

  <done>
    `scripts/ats/preview.py` has 0 `[ATS-PREVIEW]` brand markers in any docstring or help text. DSP-03 architectural commentary preserved verbatim. Python syntax valid. `--help` and `--version` continue to work. All tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/scout-run` LLM prose → user shell (cron/launchd) | New Step 7.5 + Step 9 stdout output is consumed by users via terminal or scheduled-task logs. |
| Step 7.5 validation → filesystem | Read-only checks against `report.md`, `runs.jsonl`, `new_rows.json` already in scope. No new write paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | I (Information Disclosure) | Step 7.5 + Step 9 stdout | accept | Stdout printing reveals only summary counts, timestamps, and provider outcomes — no PII, no resume_path, no `connection_names`. The Run Summary content is identical to what already lands in `report.md` (which is local-only per CON-18 PII guidance from Plan 06-03). |
| T-06-10 | T (Tampering) | Step 7.5 validation checks | accept | Validation is non-blocking — a failure prints WARNING but does not delete files, roll back, or modify state. The user is alerted; if the warning is malicious (file tampered with externally), the user investigates manually. No new attack surface. |
| T-06-11 | D (Denial of Service) | milestone-bar invocation in Step 6 + Step 9 | accept | Each invocation reads at most the last 5 lines of runs.jsonl (~10KB total). Two invocations per run × ~10ms each = negligible runtime impact on the 5-min budget. |

No new auth surface, no new PII paths, no network IO added by this plan. The grep gate Plan 06-05 verifies marketing-page Chrome scraping is gone (which is a security WIN — fewer Chrome navigations means smaller attack surface for LinkedIn JD lazy-load failures and CSRF-style bot detection).
</threat_model>

<verification>
- After Task 1: `grep -c '\\[ATS-PREVIEW\\]' skills/scout-run/SKILL.md` → 0; `grep -c '^version: 0.4.0' skills/scout-run/SKILL.md` → 1.
- After Task 2: scout-run/SKILL.md has Step 5 (d) ab_tier_counts block, Step 6 Run Summary block at top, Step 7.5 post-write validation, Step 9 stdout mirror.
- After Task 3: `grep -c '\\[ATS-PREVIEW\\]' scripts/ats/preview.py` → 0; preview.py syntax valid; `--help` works.
- Phase-wide grep gate state at end of plan: `grep -h '^version:' skills/*/SKILL.md | grep -c '0.4.0'` returns 4 (all 4 SKILL.md at v0.4.0). `grep -rn '\\[ATS-PREVIEW\\]' skills/ scripts/` returns 0 lines (Phase 6 brand cleanup complete).
- All tests still pass: `~/.job-scout-venv/bin/python3 -m pytest tests/ -q` exits 0.
</verification>

<success_criteria>
- 6 phase requirements addressed: OUT-01 (Source field already present, verified), OUT-02 (Run Summary at top of report), OUT-03 (Run Summary on stdout), OUT-04 (marketing-page deletion in scout-run/SKILL.md), CON-16 (final SKILL.md to v0.4.0 — completes 4-of-4 lockstep with Plan 06-03 + Plan 06-05 gate), CON-21 (post-write validation).
- 4 pitfalls explicitly guarded: P1 (Step 2 surgical preserves item 3), P2 ([ATS-PREVIEW] cleanup scope verified per file), P4 (ab_tier_counts wired into stats.json passthrough), P7 (Step 7.5 uses new_rows.json tier field, not grep on report).
- Locked decision D-1 (tier-aware Pass-1 share) implemented via Step 5 (d) prose; D-2 (ATS-fetch-only wall-clock) documented in Run Summary block prose.
- preview.py docstring brand-cleaned without losing DSP-03 architectural rationale.
- No regressions: all 60 prior tests + 5 Phase-6 tests pass; preview.py + dispatcher + provider modules functionally unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-04-SUMMARY.md` documenting:
- All 7 surgical edits to scout-run/SKILL.md (line ranges before + after).
- The 5 preview.py docstring edits (line numbers + before/after).
- Final scout-run/SKILL.md version + description.
- Phase-wide grep gate state: 4 SKILL.md at v0.4.0; 0 [ATS-PREVIEW] markers in skills/ + scripts/.
- Post-write validation testing approach (manual force-kill scenario noted in VALIDATION.md as out-of-scope for automated test).
- Confirmation that JSON-LD routing prose, Step 2b lazy inline detection prose, and Step 4.5 dedup prose are preserved verbatim.
- Confirmation that all 65 tests still pass.
- Any deviations from the 7-edit plan, with rationale.
</output>
