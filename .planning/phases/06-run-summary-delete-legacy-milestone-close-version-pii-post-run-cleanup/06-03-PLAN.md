---
phase: 06
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - skills/job-scout/SKILL.md
  - skills/scout-setup/SKILL.md
  - skills/scout-detect/SKILL.md
  - skills/job-scout/references/chrome-setup.md
  - .claude-plugin/plugin.json
  - README.md
autonomous: true
requirements:
  - OUT-04
  - OUT-05
  - OUT-06
  - CON-16
  - CON-17
  - CON-18
  - CON-19
user_setup: []

must_haves:
  truths:
    - "`skills/job-scout/SKILL.md` line 38 no longer enumerates schema columns inline (CON-17): `Includes company_name, industry, ...` clause replaced with a reference to `scripts/schema.py:MASTER_TARGETS_COLUMNS`"
    - "`skills/scout-setup/SKILL.md` Step 1 question 5 is followed by a PII callout that names `connections_summary.csv`, `master_targets.csv:connection_names`, `candidate_profile.json`, and `config.json:candidate.resume_path`, AND warns against iCloud / Dropbox / OneDrive (CON-18)"
    - "`skills/scout-setup/SKILL.md` Step 1 callout includes a `.gitignore` template block with at least 5 entries AND a 'redact resume_path before sharing' warning (CON-19)"
    - "`.claude-plugin/plugin.json` has `\"version\": \"0.4.0\"` (was 0.3.3) AND a v0.4 `description` field that names ATS providers — Greenhouse, Lever, Ashby, SmartRecruiters, Workday — and removes `marketing-page` style scraping language (OUT-06)"
    - "`skills/scout-setup/SKILL.md` frontmatter has `version: 0.4.0` (was 0.3.1); `skills/job-scout/SKILL.md` frontmatter has `version: 0.4.0` (was 0.3.0); `skills/scout-detect/SKILL.md` frontmatter has `version: 0.4.0` (was already 0.4.0 from Phase 3 — confirm unchanged) (CON-16)"
    - "`skills/scout-detect/SKILL.md` line 153 no longer references `[ATS-PREVIEW]` brand or hard-codes `greenhouse` — reworded to multi-provider 'ATS Pass 1 against every row where ats_provider is populated' per Pattern Map P2"
    - "`skills/job-scout/references/chrome-setup.md` does not contain marketing-page-scraping prose: `grep -c 'marketing\\|career.*page.*scrape' skills/job-scout/references/chrome-setup.md` returns 0 (OUT-04 / OUT-05)"
    - "`README.md` has a 'What's new in v0.4' section before the Requirements section, listing ATS-first sourcing + JSON-LD fallback + /scout-detect + cross-source dedup + enrich-then-tier (OUT-06)"
    - "`README.md` Pass 1 description rewritten — `pip install pandas openpyxl` becomes pipx/venv guidance (CON-04 alignment) and 'company career pages' wording is replaced with 'ATS-first sourcing'"
  artifacts:
    - path: "skills/job-scout/SKILL.md"
      provides: "CON-17: schema column reference (no inline list)"
      contains: "scripts/schema.py:MASTER_TARGETS_COLUMNS"
    - path: "skills/scout-setup/SKILL.md"
      provides: "CON-18 + CON-19: PII callout + .gitignore template + resume_path warning"
      contains: "iCloud Drive"
    - path: ".claude-plugin/plugin.json"
      provides: "OUT-06 + CON-16: v0.4.0 plugin version + updated description"
      contains: "0.4.0"
    - path: "README.md"
      provides: "OUT-06: v0.4 capabilities section"
      contains: "What's new in v0.4"
  key_links:
    - from: "skills/scout-setup/SKILL.md PII callout"
      to: "skills/job-scout/references/file-contract.md"
      via: "name-drops the same files documented as PII-bearing"
      pattern: "connections_summary.csv"
    - from: ".claude-plugin/plugin.json description"
      to: "ATS provider modules in scripts/ats/providers/"
      via: "names the 5 supported providers"
      pattern: "Greenhouse.*Lever.*Ashby.*SmartRecruiters.*Workday"
---

<objective>
Land the doc + version + PII surface for the v0.4 milestone close. Three SKILL.md files and one config file get version-bumped to 0.4.0 in lockstep (`scout-run/SKILL.md` is intentionally NOT in this plan — that file is owned by Plan 06-04, which handles all scout-run integration including its version bump). Two SKILL.md files get content surgery (`job-scout/SKILL.md` line 38 inline column list deleted; `scout-detect/SKILL.md` line 153 reworded). One reference file gets verified-clean for OUT-04/OUT-05. README gets a v0.4 capabilities section.

Purpose: Close out the v0.4 milestone's user-facing surface area. After this plan, every doc and config file outside `scout-run/SKILL.md` reflects the v0.4 reality — ATS-first, multi-provider, no marketing-page scraping. The phase-wide grep gate (Plan 06-05) will verify the lockstep.

Output: 6 files modified — 1 schema-list deletion, 1 line reword, 1 PII callout addition, 1 plugin.json bump, 1 README update, 1 chrome-setup verify. All independent of the scout-run/SKILL.md work in Plan 06-04.

This plan implements: OUT-04 (chrome-setup verified clean), OUT-05 (chrome-setup trimmed), OUT-06 (README + plugin.json), CON-16 (3-of-4 SKILL.md version bumps + plugin.json), CON-17 (job-scout/SKILL.md inline list), CON-18 (PII note), CON-19 (.gitignore template + resume_path warning).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-VALIDATION.md
@.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md
@CLAUDE.md
@skills/job-scout/SKILL.md
@skills/scout-setup/SKILL.md
@skills/scout-detect/SKILL.md
@skills/job-scout/references/chrome-setup.md
@.claude-plugin/plugin.json
@README.md

<interfaces>
<!-- Current version sprawl (verified 2026-04-29): -->
<!--   .claude-plugin/plugin.json     "version": "0.3.3"            -> bump to 0.4.0     (this plan) -->
<!--   skills/scout-run/SKILL.md     version: 0.3.3                 -> bump to 0.4.0     (Plan 06-04) -->
<!--   skills/scout-setup/SKILL.md   version: 0.3.1                 -> bump to 0.4.0     (this plan) -->
<!--   skills/job-scout/SKILL.md     version: 0.3.0                 -> bump to 0.4.0     (this plan) -->
<!--   skills/scout-detect/SKILL.md  version: 0.4.0  (already)       -> NO CHANGE         (this plan verifies) -->

<!-- After the phase: grep -h "^version:" skills/*/SKILL.md | grep -c "0.4.0" must equal 4 -->
<!-- Of those 4, this plan delivers 2 bumps (scout-setup, job-scout) + 1 verify (scout-detect already 0.4.0). -->
<!-- Plan 06-04 delivers the scout-run bump as part of its integration work. -->

<!-- Surgical text targets — use these EXACT before/after pairs: -->

CON-17 target — `skills/job-scout/SKILL.md` line 38 (current text):
```
- `MASTER_TARGETS_COLUMNS` — the master_targets.csv schema (v4). Includes `company_name`, `industry`, `career_page_url`, **`ats_provider`**, **`ats_board_url`**, `connection_names`, `linkedin_connection_count`, `application_status`, `fit_notes`, `last_checked`, `data_source`, `ats_slug_confidence`, `last_ats_hit_date`. Use `scripts/schema.py` as the authority; do not hardcode column names anywhere else.
```
Replace with:
```
- `MASTER_TARGETS_COLUMNS` — the master_targets.csv schema (v4). See `scripts/schema.py:MASTER_TARGETS_COLUMNS` for the canonical column list. Use `scripts/schema.py` as the authority; do not hardcode column names anywhere else.
```

scout-detect/SKILL.md line 153 target (current text):
```
  - Run [ATS-PREVIEW] Pass 1 against every row where ats_provider="greenhouse" —
    producing real ATS listings tagged source=ats:greenhouse
```
Replace with:
```
  - Run ATS Pass 1 against every row where ats_provider is populated —
    producing real ATS listings tagged source=ats:<provider>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump versions in lockstep (3 files) + delete inline column list (CON-16, CON-17)</name>
  <files>skills/job-scout/SKILL.md, skills/scout-setup/SKILL.md, skills/scout-detect/SKILL.md, .claude-plugin/plugin.json</files>

  <read_first>
    - skills/job-scout/SKILL.md (specifically lines 1–10 frontmatter + line 38 surgery target)
    - skills/scout-setup/SKILL.md (lines 1–10 frontmatter)
    - skills/scout-detect/SKILL.md (lines 1–10 frontmatter — verify already 0.4.0; line 153 reword target)
    - .claude-plugin/plugin.json (whole file — 8 lines)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 425–500 — version-bump lockstep details, CON-17 surgery, scout-detect line 153 surgery)
  </read_first>

  <action>
Four edits in this task — execute as separate Edit/Write operations since they touch disjoint files:

1. **`skills/job-scout/SKILL.md`** (CON-16 + CON-17):
   - Line 9 frontmatter: change `version: 0.3.0` → `version: 0.4.0`.
   - Line 38: replace the entire `MASTER_TARGETS_COLUMNS` bullet line with the post-CON-17 form (see `<interfaces>` block above — exact before/after text). The replacement collapses the `Includes ...` enumeration to a `See scripts/schema.py:MASTER_TARGETS_COLUMNS` reference. Per Pattern Map P5 + 06-PATTERNS.md note: `career_page_url` MUST stay in the body text everywhere ELSE in this file (it is a valid schema column); CON-17 only removes the inline ENUMERATION at line 38.

2. **`skills/scout-setup/SKILL.md`** (CON-16):
   - Line 5 frontmatter: change `version: 0.3.1` → `version: 0.4.0`.
   - No content surgery in this task — the PII callout is Task 2.

3. **`skills/scout-detect/SKILL.md`** (CON-16 verify + scout-detect line 153 reword per OUT-04/OUT-05/[ATS-PREVIEW] cleanup):
   - Line 5 frontmatter: VERIFY `version: 0.4.0` already present (Phase 3 set this). Do NOT re-bump if already 0.4.0 (Pitfall 5: version sprawl miss).
   - Line 153: replace `Run [ATS-PREVIEW] Pass 1 against every row where ats_provider="greenhouse" —` with `Run ATS Pass 1 against every row where ats_provider is populated —`. Adjust the next-line continuation to read `producing real ATS listings tagged source=ats:<provider>` (per `<interfaces>` block).

4. **`.claude-plugin/plugin.json`** (CON-16 + OUT-06):
   - Change `"version": "0.3.3"` → `"version": "0.4.0"`.
   - Change `"description"` from current `"Multi-source job search (career pages, ATS boards, Wellfound, ..."` to:
     ```
     "description": "ATS-first job search across Greenhouse, Lever, Ashby, SmartRecruiters, and Workday APIs, plus JSON-LD fallback, Wellfound, Built In Seattle, YC Work at a Startup, HN Who is Hiring, and LinkedIn — with honest scoring, connection-first targeting, and ATS-focused tailoring briefs."
     ```
     Removes `(career pages, ATS boards, ...)` Chrome-scraping framing; replaces with explicit 5-provider ATS-first language. Aligns with OUT-04's deletion rationale.

DELEGATE plugin.json description rewrite to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read `.claude-plugin/plugin.json` and 06-RESEARCH.md (the OUT-06 context).
2. Call `mcp__cf-code-assistant__generateCode` with description "Rewrite plugin.json description to ATS-first v0.4 framing — names 5 providers, removes Chrome scraping language" and pass current description as context.
3. Validate JSON via `python3 -c "import json; json.load(open('.claude-plugin/plugin.json'))"` after edit.

If cf-code-assistant unavailable, write directly per the explicit string above.
  </action>

  <verify>
    <automated>~/bin/bash -c 'set -e; /usr/bin/grep -h "^version:" skills/*/SKILL.md | /usr/bin/grep -c "0.4.0" | tee /tmp/_v_count; test "$(cat /tmp/_v_count)" = "3"; python3 -c "import json; d=json.load(open(\".claude-plugin/plugin.json\")); assert d[\"version\"]==\"0.4.0\", d; assert \"Greenhouse\" in d[\"description\"], \"description missing Greenhouse\""'</automated>
  </verify>

  <acceptance_criteria>
    - `/usr/bin/grep -h "^version:" skills/*/SKILL.md | /usr/bin/grep -c "0.4.0"` returns `3` after this task (scout-run still 0.3.3 — Plan 06-04 owns it; the phase-wide gate in Plan 06-05 confirms `4` after Plan 06-04 lands).
    - `/usr/bin/grep -c "version: 0.3" skills/job-scout/SKILL.md skills/scout-setup/SKILL.md` returns 0 (both bumped).
    - `/usr/bin/grep "version: 0.4.0" skills/scout-detect/SKILL.md` succeeds (unchanged from Phase 3 — Pitfall 5 verified).
    - `python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert d['version']=='0.4.0'"` exits 0.
    - `python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert 'Greenhouse' in d['description'] and 'Lever' in d['description'] and 'Ashby' in d['description'] and 'SmartRecruiters' in d['description'] and 'Workday' in d['description']"` exits 0 (5 providers named).
    - `/usr/bin/grep -c "career pages, ATS boards" .claude-plugin/plugin.json` returns 0 (old Chrome-scraping framing removed).
    - `/usr/bin/grep -c "Includes \`company_name\`" skills/job-scout/SKILL.md` returns 0 (CON-17 inline list deleted).
    - `/usr/bin/grep -c "scripts/schema.py:MASTER_TARGETS_COLUMNS" skills/job-scout/SKILL.md` returns at least 1 (CON-17 reference present).
    - `/usr/bin/grep -c "career_page_url" skills/job-scout/SKILL.md` is unchanged or reduced compared to pre-edit count — column-name references in body text outside line 38 are PRESERVED (CON-17 surgery is line-38-only).
    - `/usr/bin/grep -n "\\[ATS-PREVIEW\\]" skills/scout-detect/SKILL.md` returns 0 lines (Pattern Map P2 — line 153 reworded, no other [ATS-PREVIEW] in this file).
    - `/usr/bin/grep -c 'ats_provider="greenhouse"' skills/scout-detect/SKILL.md` returns 0 (line 153 was the only instance of hardcoded `greenhouse` in this file's prose).
    - `/usr/bin/grep -c "ats_provider is populated" skills/scout-detect/SKILL.md` returns at least 1 (line 153 reworded).
  </acceptance_criteria>

  <done>
    Three SKILL.md files at v0.4.0 (or already at 0.4.0 verified). plugin.json at v0.4.0 with ATS-first description naming all 5 providers. job-scout/SKILL.md line 38 inline column list replaced with schema.py reference. scout-detect/SKILL.md line 153 reworded to multi-provider language without [ATS-PREVIEW] brand.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PII callout + .gitignore template + resume_path warning to scout-setup/SKILL.md (CON-18, CON-19)</name>
  <files>skills/scout-setup/SKILL.md</files>

  <read_first>
    - skills/scout-setup/SKILL.md (full file — verify Step 1 question 5 location lines 51–55, identify the `---` separator at line 56 as insertion anchor)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-RESEARCH.md (lines 305–347 — exact PII callout + .gitignore template content)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 422–465 — PII callout placement + content)
    - skills/job-scout/references/file-contract.md (cross-reference: PII callout names files that match the contract)
  </read_first>

  <action>
EDIT `skills/scout-setup/SKILL.md` to insert a PII + `.gitignore` callout immediately AFTER Step 1 question 5 (the data directory question, lines 51–55) and BEFORE the `---` separator at line 56.

The callout uses Markdown blockquote syntax (`> ` prefix on every line) so it visually distinguishes from question prose. Insert this block verbatim (see 06-PATTERNS.md lines 432–465 for the canonical text):

```markdown

> **Important — PII and data directory security (v0.4):**
>
> Your `<data_dir>` contains sensitive data:
> - `connections_summary.csv` and `master_targets.csv:connection_names` — LinkedIn
>   connection data for potentially hundreds of third parties (names, companies,
>   titles) who did not consent to be included.
> - `candidate_profile.json` — your professional profile with salary targets and
>   skill assessments.
> - `config.json:candidate.resume_path` — absolute path to your resume.
>
> **Do NOT place `<data_dir>` in:**
> - iCloud Drive (syncs to Apple servers automatically)
> - Dropbox / OneDrive / Google Drive (same risk)
> - Any folder that auto-syncs to a shared device
>
> Recommended: keep `<data_dir>` at `~/Documents/JobSearch/` (local-only on macOS
> by default unless you've enabled iCloud Desktop sync).
>
> **If you ever share `config.json` (bug report, support thread, etc.):**
> Always redact `candidate.resume_path` first — it exposes your filesystem layout.
>
> A `.gitignore` template to prevent accidental git commits of your job search data:
>
> ```
> # Job Scout data directory — contains PII and personal data
> <data_dir>/
> *.csv
> *.xlsx
> config.json
> candidate_profile.json
> runs.jsonl
> ```
>
> Add this to the `.gitignore` of any project folder that's a parent of your `<data_dir>`.

```

Important formatting notes:
- The `.gitignore` template uses fenced code blocks INSIDE the blockquote (every fence line starts with `> ` followed by ` ``` `). Test by visually rendering the SKILL.md after the edit — the code block must render as a code block, not as inline text.
- The leading blank blockquote line (`> ` then a newline) before each new paragraph maintains list/paragraph separation in Markdown renderers.
- Insert exactly one blank line BEFORE the callout (separating from question 5 prose) and exactly one blank line AFTER (separating from `---`).

DELEGATE this content insertion to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read `skills/scout-setup/SKILL.md` lines 51–60 first to confirm the insertion anchor.
2. Call `mcp__cf-code-assistant__generateCode` with description "Insert PII + .gitignore callout into Markdown after specific anchor line" + the read context as `context`.
3. Review the generated insertion (Markdown formatting is fragile — verify blockquote prefix on every line including the fenced code block lines).
4. Apply via Edit tool, preserving the rest of the file byte-for-byte.

If cf-code-assistant unavailable, write directly per the verbatim block above.

DO NOT delete or modify any pre-existing prose in scout-setup/SKILL.md. The Phase 5 question 5 + Phase 1 question 4 (legacy data dir prompt) MUST remain intact.
  </action>

  <verify>
    <automated>~/bin/bash -c 'set -e; /usr/bin/grep -c "iCloud Drive" skills/scout-setup/SKILL.md | /usr/bin/grep -q "^[1-9]"; /usr/bin/grep -c "Dropbox / OneDrive" skills/scout-setup/SKILL.md | /usr/bin/grep -q "^[1-9]"; /usr/bin/grep -c "candidate.resume_path" skills/scout-setup/SKILL.md | /usr/bin/grep -q "^[1-9]"; /usr/bin/grep -c "Job Scout data directory" skills/scout-setup/SKILL.md | /usr/bin/grep -q "^[1-9]"; /usr/bin/grep -c "PII" skills/scout-setup/SKILL.md | /usr/bin/grep -q "^[1-9]"'</automated>
  </verify>

  <acceptance_criteria>
    - `/usr/bin/grep -c "iCloud Drive" skills/scout-setup/SKILL.md` ≥ 1 (CON-18 — names cloud-sync risk).
    - `/usr/bin/grep -c "Dropbox / OneDrive" skills/scout-setup/SKILL.md` ≥ 1 (CON-18 — names additional sync providers).
    - `/usr/bin/grep -c "connections_summary.csv" skills/scout-setup/SKILL.md` ≥ 1 (CON-18 — names PII file).
    - `/usr/bin/grep -c "connection_names" skills/scout-setup/SKILL.md` ≥ 1 (CON-18 — names PII column).
    - `/usr/bin/grep -c "candidate.resume_path" skills/scout-setup/SKILL.md` ≥ 1 (CON-19 — names resume_path).
    - `/usr/bin/grep -c "redact" skills/scout-setup/SKILL.md` ≥ 1 (CON-19 — explicit redaction guidance).
    - `/usr/bin/grep -c "Job Scout data directory" skills/scout-setup/SKILL.md` ≥ 1 (CON-19 — .gitignore template comment line).
    - `/usr/bin/grep -c "^> \\*csv" skills/scout-setup/SKILL.md` is 0 (negative check — confirm we're not creating malformed glob patterns).
    - The callout appears AFTER Step 1's question 5 — verify by `awk '/^5\\. \\*\\*Data directory\\*\\*/{flag=1;next} /^---$/{flag=0} flag' skills/scout-setup/SKILL.md | /usr/bin/grep -c "iCloud Drive"` is ≥ 1 (matches between question 5 and `---`).
    - File is still valid Markdown — `/usr/bin/grep -c "^---$" skills/scout-setup/SKILL.md` count is unchanged from pre-edit (no extra/missing `---` separators).
    - Step 6 (Write the state pointer) and downstream steps are byte-identical to pre-edit — `/usr/bin/grep -A 5 "## Step 6: Write the state pointer" skills/scout-setup/SKILL.md` shows the same body.
  </acceptance_criteria>

  <done>
    `skills/scout-setup/SKILL.md` Step 1 has a new PII callout + `.gitignore` template block inserted after question 5. The callout names `connections_summary.csv`, `master_targets.csv:connection_names`, `candidate_profile.json`, `config.json:candidate.resume_path`. iCloud / Dropbox / OneDrive / Google Drive named as do-not-place locations. `.gitignore` template included with at least 5 entries plus the `Job Scout data directory` comment header. Resume path redaction warning present. Steps 2–8 unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: README v0.4 capabilities section + verify chrome-setup.md (OUT-04, OUT-05, OUT-06)</name>
  <files>README.md, skills/job-scout/references/chrome-setup.md</files>

  <read_first>
    - README.md (full file, ~125 lines — confirm structure)
    - skills/job-scout/references/chrome-setup.md (full file, ~145 lines — verify already LinkedIn-only post-Phase-5)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-PATTERNS.md (lines 505–545 — README v0.4 section content + Pass 1 surgery)
    - .planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-VALIDATION.md (lines 43–44 — OUT-04 grep gates)
  </read_first>

  <action>
**Edit 1: `skills/job-scout/references/chrome-setup.md` (OUT-04 / OUT-05 — VERIFY CLEAN, surgical trim if needed):**

This file was scoped to LinkedIn-only in Phase 5 (CON-11/CON-12). Verify it does not contain `marketing-page` scraping prose:
- `/usr/bin/grep -ni "marketing-page\\|marketing page" skills/job-scout/references/chrome-setup.md` MUST return 0.
- `/usr/bin/grep -ni "career.*page.*scrape\\|scrape.*career.*page" skills/job-scout/references/chrome-setup.md` MUST return 0.

If grep returns matches, surgically edit to remove the offending prose. The current state (line 72) has one borderline phrase `Career pages and ATS boards (Greenhouse, Lever, Workday, Ashby) generally return full JDs on first navigation — no scroll-and-expand dance required.` This is EXPLANATORY (says career pages return full JDs without lazy-load) and is NOT scraping prose — it does not instruct any Chrome action against career pages, it observes that the LinkedIn lazy-load dance does not apply to non-LinkedIn pages. KEEP this line — it's documenting why the dance is LinkedIn-specific. Pitfall 1 from RESEARCH (line 396) explicitly notes this borderline line.

Per VALIDATION.md grep gate: `/usr/bin/grep -c "marketing\\|career.*page.*scrape" skills/job-scout/references/chrome-setup.md` MUST return 0. If the existing file already passes this gate (verify by running the grep), no edit is needed. If it doesn't pass (e.g., the borderline line at 72 trips on `career.*page.*scrape` — it does not, since the regex requires `scrape` and the line says `dance`), tighten the wording.

If the borderline line at 72 needs softening for safety: replace with `Career pages and ATS boards generally return full JDs on first navigation — no LinkedIn-specific lazy-load dance required.` (replaces the literal provider name list with the abstract concept; reduces grep false-positive risk).

**Edit 2: `README.md` (OUT-06 — v0.4 capabilities section + Pass 1 rewrite):**

Three surgical edits:

(a) Lines 9–10 — replace the current Pass 1 description:
```
1. **Pass 1 — Company-first deep-dive** (60% of effort). For each of your top target companies (sorted by warm connections × recency), it checks the company's own career page, detects the ATS provider (Greenhouse / Lever / Workday / Ashby), and falls back to LinkedIn's company jobs tab.
```
With:
```
1. **Pass 1 — ATS-first sourcing** (60% of effort). For each of your top target companies, queries structured ATS APIs directly — Greenhouse, Lever, Ashby, SmartRecruiters, Workday — plus a JSON-LD fallback for companies with no detected ATS. Then runs a LinkedIn keyword search scoped to each company name. Marketing-page Chrome scraping was removed in v0.4.
```

(b) Line 21 — replace `pip install pandas openpyxl`:
```
- Python 3.8+ with `pandas` and `openpyxl` (`pip install pandas openpyxl`)
```
With:
```
- Python 3.8+ with `pandas`, `openpyxl`, `httpx>=0.27,<0.29`, and `rapidfuzz` (use `pipx install` or `python3 -m venv`; the scripts print install hints on first run)
```

(c) Insert a new `## What's new in v0.4` section BETWEEN `## What it does` (ending around line 13) and `## Requirements` (starting at line 15). Insert verbatim per 06-PATTERNS.md lines 525–545:

```markdown

## What's new in v0.4

- **ATS-first sourcing.** Queries Greenhouse, Lever, Ashby, SmartRecruiters, and Workday APIs directly — no Chrome navigation of marketing pages.
- **JSON-LD fallback.** Companies with no detected ATS provider get a structured JSON-LD fetch on their `career_page_url` (no Chrome).
- **`/scout-detect` skill.** Batch + lazy-inline detection of each company's ATS provider. Populates `ats_provider`, `ats_board_url`, and `ats_slug_confidence` in `master_targets.csv` with a two-factor name-match gate.
- **Cross-source dedup.** ATS and LinkedIn listings are deduplicated with two-key fuzzy matching (`rapidfuzz`). Decisions are logged to `runs.jsonl` for transparency; borderline matches surface in the report's *Honest notes*.
- **Enrich-then-tier.** ATS A/B-tier candidates get LinkedIn shared-connection lookup BEFORE final tier assignment (restores warm-path signal lost by pure ATS results).
- **Structured observability.** `runs.jsonl` records per-provider outcomes, field completion, dedup decisions, A/B-tier counts per source, regression suspects, and Pass-2 board-broken warnings.
- **Milestone bar.** `python3 scripts/ats/runs_log.py milestone-bar <data_dir>/runs.jsonl --lookback 5` prints a JSON dict with `pass1_share_pct` (target ≥ 60%) and `wall_clock_avg_seconds` (target ≤ 300s, ATS-fetch only — not total `/scout-run` wall-clock).

```

(d) Append a new `0.4.0` entry at the TOP of the existing `## Versioning` list (currently lines 122–125). Insert as the first bullet before `**0.3.0** — ...`:

```markdown
- **0.4.0** — ATS-first sourcing across Greenhouse, Lever, Ashby, SmartRecruiters, Workday + JSON-LD fallback. New `/scout-detect` skill. Cross-source dedup. Enrich-then-tier. Marketing-page Chrome scraping removed. Milestone bar verifiable from `runs.jsonl`.
```

DELEGATE the README v0.4 capabilities section content to cf-code-assistant if available (per CLAUDE.md model routing):
1. Read README.md (full) and 06-RESEARCH.md (for v0.4 capability list).
2. Call `mcp__cf-code-assistant__generateCode` with description "Write a 'What's new in v0.4' section for the Job Scout README that summarizes Phase 1-5 deliverables" + the read context as `context`.
3. Review the generated section against the 06-PATTERNS.md template; integrate via Edit tool — keep the rest of the README byte-identical.

If cf-code-assistant unavailable, write directly per the verbatim block above.

Do NOT modify the `## Components`, `## File outputs`, `## Scoring philosophy`, or `## Customization` sections. They remain accurate for v0.4.
  </action>

  <verify>
    <automated>~/bin/bash -c 'set -e; test "$(/usr/bin/grep -c "marketing\\|career.*page.*scrape" skills/job-scout/references/chrome-setup.md)" = "0"; /usr/bin/grep -q "What.s new in v0.4" README.md; /usr/bin/grep -q "ATS-first sourcing" README.md; /usr/bin/grep -q "milestone-bar" README.md; /usr/bin/grep -q "0.4.0" README.md; /usr/bin/grep -q "JSON-LD fallback" README.md; /usr/bin/grep -q "rapidfuzz" README.md'</automated>
  </verify>

  <acceptance_criteria>
    - `/usr/bin/grep -c "marketing\\|career.*page.*scrape" skills/job-scout/references/chrome-setup.md` returns 0 (OUT-04 grep gate per VALIDATION.md).
    - `/usr/bin/grep -c "marketing-page\\|marketing page" skills/job-scout/references/chrome-setup.md` returns 0 (OUT-04 alternate gate).
    - `/usr/bin/grep -c "## What's new in v0.4" README.md` is exactly 1.
    - `/usr/bin/grep -c "ATS-first sourcing" README.md` is at least 2 (Pass 1 description + What's new section).
    - `/usr/bin/grep -c "Greenhouse, Lever, Ashby, SmartRecruiters" README.md` is at least 1 (5-provider list named).
    - `/usr/bin/grep -c "JSON-LD fallback" README.md` is at least 1.
    - `/usr/bin/grep -c "/scout-detect" README.md` is at least 1 (new skill named).
    - `/usr/bin/grep -c "milestone-bar" README.md` is at least 1 (Phase 6 measurement helper named).
    - `/usr/bin/grep -c "rapidfuzz" README.md` is at least 1 (dependency named).
    - `/usr/bin/grep -c "pipx install\\|python3 -m venv" README.md` is at least 1 (CON-04 alignment in install hint).
    - `/usr/bin/grep -c "0.4.0" README.md` is at least 1 (Versioning section entry).
    - `/usr/bin/grep -c "checks the company's own career page, detects" README.md` returns 0 (old Pass 1 wording removed).
    - `/usr/bin/grep -c "pip install pandas openpyxl" README.md` returns 0 (old install hint removed).
    - File contract sections (`## Components`, `## File outputs`, `## Scoring philosophy`, `## Customization`) byte-identical to pre-edit (verify with `diff` after stash/restore if necessary).
  </acceptance_criteria>

  <done>
    chrome-setup.md passes the OUT-04 grep gate (0 marketing-page references). README has a new `## What's new in v0.4` section with all 7 capability bullets. README Pass 1 description rewritten to ATS-first language. Install hint updated to pipx/venv. Versioning section has new 0.4.0 entry at the top.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing skill prose → user terminal | scout-setup callout names PII files but does not exfiltrate them; user reads it during setup. |
| `.gitignore` template → user's git workflow | Documentation only; user copies template to their own `.gitignore`. The plugin doesn't write `.gitignore` for them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | I (Information Disclosure) | scout-setup PII callout | mitigate | The callout itself is the mitigation: it warns the user about PII risks (CON-18) and provides a concrete .gitignore template (CON-19) so the user can opt out of accidental exposure. No new code path leaks PII. |
| T-06-07 | I (Information Disclosure) | README.md v0.4 section | accept | README contains no PII or secrets — capability summary only. |
| T-06-08 | T (Tampering) | plugin.json | accept | Single-file version + description bump. Schema-validated by `python3 -c "import json; json.load(...)"` post-edit. |

No new auth surface, no new write paths, no new network calls. Documentation-only PII mitigation per ASVS V8 (Data Protection — user-facing risk awareness).
</threat_model>

<verification>
- `/usr/bin/grep -h "^version:" skills/*/SKILL.md | /usr/bin/grep -c "0.4.0"` returns 3 (this plan's contribution; Plan 06-04 lifts to 4).
- `python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert d['version']=='0.4.0' and 'Greenhouse' in d['description']"` exits 0.
- `/usr/bin/grep -c "Includes \`company_name\`" skills/job-scout/SKILL.md` returns 0 (CON-17).
- `/usr/bin/grep -ic "PII" skills/scout-setup/SKILL.md` ≥ 1 (CON-18).
- `/usr/bin/grep -c "candidate.resume_path" skills/scout-setup/SKILL.md` ≥ 1 (CON-19).
- `/usr/bin/grep -c "marketing\\|career.*page.*scrape" skills/job-scout/references/chrome-setup.md` returns 0 (OUT-04/OUT-05).
- `/usr/bin/grep -c "What's new in v0.4" README.md` is 1 (OUT-06).
</verification>

<success_criteria>
- 12 of 12 acceptance criteria across 3 tasks pass.
- 7 phase requirements addressed: OUT-04, OUT-05, OUT-06, CON-16 (3-of-4 SKILL.md bumps; final SKILL.md is Plan 06-04), CON-17, CON-18, CON-19.
- No regressions: pre-existing `/scout-setup`, `/scout-detect`, `/scout-run`, and core skill prose (`job-scout/SKILL.md` Steps 1–8) are byte-identical except for the surgical changes documented above.
- `pytest tests/ -q` continues to pass (no test impact since this plan is doc-only — no Python code changed).
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-summary-delete-legacy-milestone-close-version-pii-post-run-cleanup/06-03-SUMMARY.md` documenting:
- 6 files modified (list paths).
- Version state after this plan (3 SKILL.md at 0.4.0, plugin.json at 0.4.0, scout-run still 0.3.3 awaiting Plan 06-04).
- CON-17 surgery confirmation (line 38 inline list deleted).
- CON-18 + CON-19 callout location (Step 1 question 5 → callout → `---` separator).
- chrome-setup.md verification result (0 grep matches for marketing-page prose; any softening edits if applied).
- README v0.4 section anchor location.
- Any deviations from the planned content.
</output>
