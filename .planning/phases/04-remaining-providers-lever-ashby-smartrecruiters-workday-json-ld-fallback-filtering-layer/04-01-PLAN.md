---
phase: 04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ats/providers/base.py
  - scripts/ats/detect.py
  - tests/test_providers_phase4.py
  - tests/fixtures/ats/lever/__init__.py
  - tests/fixtures/ats/lever/spotify.json
  - tests/fixtures/ats/ashby/__init__.py
  - tests/fixtures/ats/ashby/ashby.json
  - tests/fixtures/ats/smartrecruiters/__init__.py
  - tests/fixtures/ats/smartrecruiters/visa.json
  - tests/fixtures/ats/workday/__init__.py
  - tests/fixtures/ats/workday/workday_wd5.json
  - tests/fixtures/ats/workday/workday_synthetic_wd1.json
  - tests/fixtures/ats/workday/workday_synthetic_wd3.json
  - tests/fixtures/ats/workday/SOURCE.md
  - tests/fixtures/jsonld/__init__.py
  - tests/fixtures/jsonld/example_careers.html
autonomous: true
requirements: [PRV-05, PRV-09, STR-01]
must_haves:
  truths:
    - "FetchResult dataclass has auth_required: bool = False field; existing call sites unaffected (default propagates)."
    - "detect.py PROVIDERS-iteration loop skips providers whose BOARD_URL_PATTERNS is empty (D-3 invariant)."
    - "tests/test_providers_phase4.py exists with 15 test functions; all currently FAIL (RED) since providers + filters not implemented yet."
    - "5 fixture directories exist with __init__.py + sanitized fixture data (lever, ashby, smartrecruiters, workday, jsonld)."
    - "Workday fixtures include 1 live-verified (wd5) + 2 synthetic (wd1, wd3) with SOURCE.md provenance documenting D-4."
  artifacts:
    - path: "scripts/ats/providers/base.py"
      provides: "FetchResult.auth_required field added"
      contains: "auth_required: bool = False"
    - path: "scripts/ats/detect.py"
      provides: "Empty BOARD_URL_PATTERNS guard in PROVIDERS iteration"
      contains: "if not getattr(provider, \"BOARD_URL_PATTERNS\", None):"
    - path: "tests/test_providers_phase4.py"
      provides: "15 RED test functions covering PRV-01..09, STR-01, STR-03"
      min_lines: 250
    - path: "tests/fixtures/ats/lever/spotify.json"
      provides: "Bare JSON array Lever fixture (3 jobs, sanitized)"
      contains: "createdAt"
    - path: "tests/fixtures/ats/ashby/ashby.json"
      provides: "Ashby fixture with at least one isListed=false job"
      contains: "isListed"
    - path: "tests/fixtures/ats/smartrecruiters/visa.json"
      provides: "Combined list+detail SR fixture"
      contains: "jobAd"
    - path: "tests/fixtures/ats/workday/workday_wd5.json"
      provides: "Live-verified Workday wd5 jobPostings fixture"
      contains: "Posted"
    - path: "tests/fixtures/ats/workday/SOURCE.md"
      provides: "Provenance for wd5 (live) + wd1/wd3 (synthetic) per D-4"
      contains: "Synthetic"
    - path: "tests/fixtures/jsonld/example_careers.html"
      provides: "schema.org/JobPosting HTML fixture"
      contains: "application/ld+json"
  key_links:
    - from: "scripts/ats/providers/base.py"
      to: "scripts/ats/providers/workday.py (Wave 2 / Plan 04-03)"
      via: "auth_required field on FetchResult"
      pattern: "auth_required: bool = False"
    - from: "scripts/ats/detect.py"
      to: "scripts/ats/providers/jsonld.py (Wave 2 / Plan 04-04)"
      via: "Empty BOARD_URL_PATTERNS skip guard"
      pattern: "BOARD_URL_PATTERNS"
    - from: "tests/test_providers_phase4.py"
      to: "all 5 provider modules (Wave 2) + normalize.py filters (Wave 3)"
      via: "RED tests turn GREEN as Wave 2/3 modules land"
      pattern: "from ats.providers import"
---

<objective>
Wave 0 scaffolding for Phase 4. Land three things atomically so all downstream plans (04-02, 04-03, 04-04, 04-05) can be implemented test-first against a frozen contract:

1. **Additive `auth_required` field on FetchResult** (D-1 locked) — workday.py in Plan 04-03 needs this; landing it now means workday.py can be written + tested without modifying base.py.
2. **`detect.py` skip-empty-BOARD_URL_PATTERNS guard** (D-3 locked) — jsonld.py in Plan 04-04 will be registered with `BOARD_URL_PATTERNS=[]`; this guard prevents detection from probing it spuriously.
3. **`tests/test_providers_phase4.py` + 5 fixture sets** — 15 RED test functions covering every PRV-01..09, STR-01, STR-03 acceptance behavior. Tests fail now (no provider modules); they turn GREEN as Wave 2/3 plans land. This is the enforcement substrate that prevents Wave 2 implementations from drifting from VALIDATION.md's behavioral contract.

**Purpose:** All Wave 2 plans (04-02, 04-03, 04-04) consume this scaffolding. Without it, each provider plan would have to re-derive its own test harness and the auth_required contract would have to be revised mid-Wave-2 (causing a base.py file conflict between Plan 04-03 and Plan 04-05). Landing it together in Wave 1 keeps the rest of the phase parallel.

**Output:** base.py with the new field, detect.py with the new guard, one pytest module, 5 fixture trees, and a SOURCE.md documenting D-4 (synthetic Workday wd1/wd3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-RESEARCH.md
@.planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-PATTERNS.md
@.planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scripts/ats/providers/base.py
@scripts/ats/detect.py
@scripts/ats/providers/greenhouse.py
@tests/test_detection.py
@tests/conftest.py
@tests/fixtures/ats/greenhouse/airbnb.json
@tests/fixtures/ats/greenhouse/SOURCE.md

<interfaces>
<!-- Frozen contracts other Phase 4 plans consume from this plan. -->

From scripts/ats/providers/base.py (BEFORE this plan — current shape):
```python
@dataclass(frozen=True)
class FetchResult:
    provider: str
    company_slug: str
    listings: List["Listing"]
    raw: List[Dict[str, Any]]
    http_status: int
```

After this plan (frozen contract for Plan 04-03 workday.py):
```python
@dataclass(frozen=True)
class FetchResult:
    provider: str
    company_slug: str
    listings: List["Listing"]
    raw: List[Dict[str, Any]]
    http_status: int
    # PRV-05 / D-1: Workday CSRF/auth-required signal. Default False — all
    # existing providers (greenhouse) inherit without change.
    auth_required: bool = False
```

From scripts/ats/detect.py PROVIDERS iteration (lines ~455-470 BEFORE this plan):
```python
for provider_name, provider in PROVIDERS.items():
    sem = _DET_SEMAPHORES.get(provider_name)
    ...
```

After this plan (frozen contract for Plan 04-04 jsonld.py registration):
```python
for provider_name, provider in PROVIDERS.items():
    # D-3 (locked): skip providers with empty BOARD_URL_PATTERNS.
    # jsonld.py has BOARD_URL_PATTERNS=[] — it is a fallback, not detectable.
    if not getattr(provider, "BOARD_URL_PATTERNS", None):
        continue
    sem = _DET_SEMAPHORES.get(provider_name)
    ...
```

Existing Listing contract (no change — read from scripts/ats/normalize.py):
```python
REQUIRED_FIELDS = ("company", "title", "location", "url", "posted_date", "source")
# Listing.__post_init__ raises ValueError if any required field is empty/None.
```

Existing fixture pattern (from tests/fixtures/ats/greenhouse/SOURCE.md):
- Each provider directory: __init__.py (empty package marker) + <company>.json (sanitized payload) + SOURCE.md (provenance + sanitization log)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth_required field to FetchResult + add D-3 skip guard in detect.py</name>
  <files>scripts/ats/providers/base.py, scripts/ats/detect.py</files>
  <read_first>
    - scripts/ats/providers/base.py (full file, ~124 lines — full FetchResult shape)
    - scripts/ats/detect.py lines 440-510 (PROVIDERS iteration loop where guard goes)
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-PATTERNS.md "scripts/ats/providers/base.py — MODIFY" + "scripts/ats/detect.py — MODIFY" sections
  </read_first>
  <behavior>
    - base.py FetchResult dataclass gains a new field: `auth_required: bool = False` placed AFTER `http_status: int` (last field) so it has a default and existing positional call sites (e.g. greenhouse.py line ~278) remain valid.
    - Existing greenhouse.py + dispatcher.py + smoke tests must continue to pass (the field has a default, so old constructors `FetchResult(provider=..., company_slug=..., listings=..., raw=..., http_status=...)` still work — Python fills auth_required=False).
    - detect.py PROVIDERS iteration loop (per D-3 locked decision) skips any provider where `BOARD_URL_PATTERNS` is empty/falsy via `if not getattr(provider, "BOARD_URL_PATTERNS", None): continue` placed at the TOP of the loop body, BEFORE the `sem = _DET_SEMAPHORES.get(provider_name)` line.
    - The skip is silent (no print/log) — it is part of normal operation, not an exception. jsonld.py is the only intended consumer in v0.4.
  </behavior>
  <action>
    Per D-1 locked decision: in `scripts/ats/providers/base.py` modify the `FetchResult` dataclass to add ONE new field at the end:

    ```python
    @dataclass(frozen=True)
    class FetchResult:
        provider: str
        company_slug: str
        listings: List["Listing"]
        raw: List[Dict[str, Any]]
        http_status: int
        # PRV-05 / D-1: Workday CSRF/auth-required signal. True when provider
        # receives 401/403 with CSRF/session markers in body. Dispatcher logs
        # this as "workday_auth_required" reason in runs.jsonl rather than
        # generic ERROR. Default False — all existing providers (greenhouse)
        # inherit without changing call sites.
        auth_required: bool = False
    ```

    Add a docstring sentence to FetchResult's existing docstring noting the new field's purpose.

    Per D-3 locked decision: in `scripts/ats/detect.py`, find the `for provider_name, provider in PROVIDERS.items():` loop in `_detect_company_against_providers` (around line 455 — confirm exact line by reading file). Insert this guard at the TOP of the loop body, BEFORE the `sem = _DET_SEMAPHORES.get(provider_name)` line:

    ```python
        for provider_name, provider in PROVIDERS.items():
            # D-3 (locked Phase 4 decision): skip providers with empty
            # BOARD_URL_PATTERNS. jsonld.py has BOARD_URL_PATTERNS=[] — it is
            # a fallback, not a detectable ATS. Probing it would always return
            # NOT_FOUND and waste a network call.
            if not getattr(provider, "BOARD_URL_PATTERNS", None):
                continue
            sem = _DET_SEMAPHORES.get(provider_name)
    ```

    Do NOT modify any greenhouse.py or dispatcher.py call sites — both keyword and positional FetchResult constructions continue to work because auth_required has a default.

    Run the existing Phase 2 + 3 test suite to confirm zero regressions (test_migration.py + test_detection.py must still pass).
  </action>
  <verify>
    <automated>~/.job-scout-venv/bin/python3 -m pytest tests/test_migration.py tests/test_detection.py -x -q && grep -c 'auth_required: bool = False' scripts/ats/providers/base.py | grep -q '^1$' && grep -c 'if not getattr(provider, "BOARD_URL_PATTERNS", None):' scripts/ats/detect.py | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'auth_required: bool = False' scripts/ats/providers/base.py` returns exactly `1`
    - `grep -c 'if not getattr(provider, "BOARD_URL_PATTERNS", None):' scripts/ats/detect.py` returns exactly `1`
    - The guard line appears LEXICALLY BEFORE the `sem = _DET_SEMAPHORES.get(provider_name)` line in detect.py: `awk '/for provider_name, provider in PROVIDERS.items/{found=1} found && /BOARD_URL_PATTERNS/{print NR" guard"; exit} found && /_DET_SEMAPHORES.get/{print NR" sem"; exit}' scripts/ats/detect.py` prints "guard"
    - `~/.job-scout-venv/bin/python3 -c "from scripts.ats.providers.base import FetchResult; r = FetchResult(provider='x', company_slug='y', listings=[], raw=[], http_status=200); assert r.auth_required is False; r2 = FetchResult(provider='x', company_slug='y', listings=[], raw=[], http_status=401, auth_required=True); assert r2.auth_required is True; print('OK')"` exits 0 and prints `OK`
    - `~/.job-scout-venv/bin/python3 -m pytest tests/test_migration.py tests/test_detection.py -x -q` exits 0 (no Phase 1/3 regressions)
  </acceptance_criteria>
  <done>
    base.py has the new field with a `False` default; detect.py skips providers with empty BOARD_URL_PATTERNS; existing test suite (Phase 1 + Phase 3) passes unchanged; greenhouse.py + dispatcher.py + preview.py call sites untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 5 fixture sets (lever, ashby, smartrecruiters, workday, jsonld) + workday SOURCE.md</name>
  <files>tests/fixtures/ats/lever/__init__.py, tests/fixtures/ats/lever/spotify.json, tests/fixtures/ats/ashby/__init__.py, tests/fixtures/ats/ashby/ashby.json, tests/fixtures/ats/smartrecruiters/__init__.py, tests/fixtures/ats/smartrecruiters/visa.json, tests/fixtures/ats/workday/__init__.py, tests/fixtures/ats/workday/workday_wd5.json, tests/fixtures/ats/workday/workday_synthetic_wd1.json, tests/fixtures/ats/workday/workday_synthetic_wd3.json, tests/fixtures/ats/workday/SOURCE.md, tests/fixtures/jsonld/__init__.py, tests/fixtures/jsonld/example_careers.html</files>
  <read_first>
    - tests/fixtures/ats/greenhouse/airbnb.json (existing fixture shape — sanitized, 3 jobs)
    - tests/fixtures/ats/greenhouse/SOURCE.md (provenance template)
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-PATTERNS.md "Fixture files" section (lines ~991-1050) for exact JSON shapes
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-RESEARCH.md (per-provider response shape sections — confirms field names: Lever `createdAt` epoch_ms, Ashby `isListed`, SR `jobAd.sections.jobDescription.text`, Workday `postedOn` "Posted N Days Ago", JSON-LD `@type` may be string or list)
  </read_first>
  <behavior>
    - Each fixture file is a sanitized, realistic representation of one provider's response shape (no real PII, no real secrets, no real candidate names).
    - Lever fixture is a BARE JSON ARRAY (not wrapped in `{"jobs": [...]}` — Lever specificity).
    - Ashby fixture is a JSON object with `jobs` array containing AT LEAST one entry with `isListed: false` (so test_ashby_filters_unlisted has data to assert against).
    - SmartRecruiters fixture is a JSON object with TWO top-level keys `list` (with content array) AND `detail` (single job detail) — combined fixture so test_sr_description_from_detail can merge them.
    - Workday wd5 fixture has `total` + `jobPostings` array with at least 1 job containing realistic `title`, `externalPath`, `locationsText`, `postedOn` (use "Posted 5 Days Ago" so date-parsing test has a value to assert against).
    - Workday wd1 + wd3 fixtures are SYNTHETIC (per D-4) — same shape as wd5 but with "Posted Today" and "Posted 30+ Days Ago" respectively for postedOn parsing coverage.
    - SOURCE.md documents which fixtures are live vs synthetic per D-4.
    - JSON-LD fixture is an HTML page containing AT LEAST ONE `<script type="application/ld+json">` block with a `JobPosting`-typed object (datePosted, title, hiringOrganization.name, jobLocation.address.addressLocality).
    - All __init__.py files are empty (zero bytes) per existing convention.
  </behavior>
  <action>
    Use the `mkdir -p` + `touch` + `Write` pattern. Each fixture follows the verbatim shape spec'd in 04-PATTERNS.md "Fixture files" section.

    **lever/spotify.json** — bare JSON array, 3 jobs, sanitized. Required fields per RESEARCH.md Lever shape:
    ```json
    [
      {"id": "1ff4a4e3-aaaa-bbbb-cccc-dddddddddddd", "text": "Senior Software Engineer", "hostedUrl": "https://jobs.lever.co/spotify/1ff4a4e3-aaaa-bbbb-cccc-dddddddddddd", "createdAt": 1773335421350, "categories": {"location": "Remote - USA", "department": "Engineering", "commitment": "Full-time"}, "descriptionPlain": "Senior backend engineer role for streaming infra."},
      {"id": "2aa5b5f4-eeee-ffff-1111-222222222222", "text": "Product Designer", "hostedUrl": "https://jobs.lever.co/spotify/2aa5b5f4-eeee-ffff-1111-222222222222", "createdAt": 1773225321000, "categories": {"location": "Stockholm, Sweden", "department": "Design", "commitment": "Full-time"}, "descriptionPlain": "Lead designer for podcasts surface."},
      {"id": "3bb6c6g5-3333-4444-5555-666666666666", "text": "Data Scientist", "hostedUrl": "https://jobs.lever.co/spotify/3bb6c6g5-3333-4444-5555-666666666666", "createdAt": 1773115221000, "categories": {"location": "New York, NY", "department": "Data Science", "commitment": "Full-time"}, "descriptionPlain": "Recommendation systems data science."}
    ]
    ```

    **ashby/ashby.json** — object with `apiVersion` + `jobs` array; AT LEAST ONE job with `isListed: false`:
    ```json
    {"apiVersion": 1, "jobs": [
      {"id": "a1b2c3d4-1111-2222-3333-444444444444", "title": "Software Engineer", "jobUrl": "https://jobs.ashbyhq.com/ashby/a1b2c3d4-1111-2222-3333-444444444444", "publishedAt": "2026-03-15T10:23:45.000Z", "location": "Remote - US", "isListed": true, "department": "Engineering", "employmentType": "FullTime", "descriptionPlain": "Engineer role for ATS infra."},
      {"id": "b2c3d4e5-5555-6666-7777-888888888888", "title": "Internal Draft Role", "jobUrl": "https://jobs.ashbyhq.com/ashby/b2c3d4e5-5555-6666-7777-888888888888", "publishedAt": "2026-03-01T00:00:00.000Z", "location": "San Francisco", "isListed": false, "department": "Engineering", "employmentType": "FullTime", "descriptionPlain": "Should be filtered out by isListed=false."},
      {"id": "c3d4e5f6-9999-aaaa-bbbb-cccccccccccc", "title": "Product Manager", "jobUrl": "https://jobs.ashbyhq.com/ashby/c3d4e5f6-9999-aaaa-bbbb-cccccccccccc", "publishedAt": "2026-04-01T00:00:00.000Z", "location": "Remote - EU", "isListed": true, "department": "Product", "employmentType": "FullTime", "descriptionPlain": "PM role for posting API."}
    ]}
    ```

    **smartrecruiters/visa.json** — combined list + detail:
    ```json
    {
      "list": {"content": [{"id": "100sr-aaa-bbb", "name": "Product Manager", "company": {"name": "Visa"}, "releasedDate": "2026-03-15T10:23:45.000Z", "location": {"city": "San Francisco", "region": "CA", "country": "US", "remote": false}, "department": {"label": "Product"}, "typeOfEmployment": {"label": "Full-time"}, "ref": "https://jobs.smartrecruiters.com/Visa/100sr-aaa-bbb"}], "totalFound": 1, "offset": 0, "limit": 100},
      "detail": {"id": "100sr-aaa-bbb", "name": "Product Manager", "company": {"name": "Visa"}, "releasedDate": "2026-03-15T10:23:45.000Z", "location": {"city": "San Francisco", "region": "CA", "country": "US"}, "department": {"label": "Product"}, "typeOfEmployment": {"label": "Full-time"}, "ref": "https://jobs.smartrecruiters.com/Visa/100sr-aaa-bbb", "jobAd": {"sections": {"jobDescription": {"text": "<p>Product manager for cross-border payments.</p>"}}}}
    }
    ```

    **workday/workday_wd5.json** — live-verified shape (per D-4):
    ```json
    {"total": 448, "jobPostings": [
      {"title": "Account Executive", "externalPath": "/job/JPNOsaka/Account-Executive_JR-0101360", "locationsText": "Osaka, Japan", "postedOn": "Posted 5 Days Ago", "remoteType": "Onsite", "bulletFields": ["JR-0101360"]},
      {"title": "Senior Engineering Manager", "externalPath": "/job/USCA-Pleasanton/Senior-Engineering-Manager_JR-0101441", "locationsText": "Pleasanton, CA", "postedOn": "Posted 10 Days Ago", "remoteType": "Hybrid", "bulletFields": ["JR-0101441"]}
    ]}
    ```

    **workday/workday_synthetic_wd1.json** — synthetic per D-4, "Posted Today":
    ```json
    {"total": 12, "jobPostings": [{"title": "Synthetic Today Role", "externalPath": "/job/USNY-NewYork/Synthetic-Today-Role_JR-9000001", "locationsText": "New York, NY", "postedOn": "Posted Today", "remoteType": "Onsite", "bulletFields": ["JR-9000001"]}]}
    ```

    **workday/workday_synthetic_wd3.json** — synthetic per D-4, "Posted 30+ Days Ago":
    ```json
    {"total": 5, "jobPostings": [{"title": "Synthetic Stale Role", "externalPath": "/job/UKLondon/Synthetic-Stale-Role_JR-9000002", "locationsText": "London, UK", "postedOn": "Posted 30+ Days Ago", "remoteType": "Onsite", "bulletFields": ["JR-9000002"]}]}
    ```

    **workday/SOURCE.md** — document D-4 provenance:
    ```markdown
    # Workday Fixture Provenance

    Per D-4 (Phase 4 locked decision): wd5 fixture is live-verified; wd1 and wd3 fixtures are SYNTHETIC because live wd1/wd3 probes returned 422 during research. Parsing logic is identical across data centers (only URL components vary), so synthetic fixtures adequately exercise to_listing() field mapping + postedOn parsing.

    | Fixture | Source | Date | Notes |
    |---------|--------|------|-------|
    | workday_wd5.json | Live probe: workday.wd5.myworkdayjobs.com/Workday, searchText="a", POST /wday/cxs/{tenant}/{site}/jobs | 2026-04-28 | Sliced to 2 representative jobs; sanitized — no real candidate PII captured |
    | workday_synthetic_wd1.json | Synthetic — modeled on wd5 live shape | 2026-04-28 | wd1 live probes returned 422 during research; D-4 locked synthetic acceptable. "Posted Today" string exercises today-parsing branch |
    | workday_synthetic_wd3.json | Synthetic — modeled on wd5 live shape | 2026-04-28 | Same rationale as wd1. "Posted 30+ Days Ago" exercises bounded-stale branch |

    ## Re-capture command (wd5 only)

    ```bash
    curl -s -X POST 'https://workday.wd5.myworkdayjobs.com/wday/cxs/workday/Workday/jobs' \
      -H 'Content-Type: application/json' \
      -d '{"appliedFacets":{},"limit":20,"offset":0,"searchText":"a"}' | python3 -m json.tool > /tmp/wd5_full.json
    ```

    Then slice to first 2 entries from `jobPostings` array, redact any candidate-identifying tokens that appear in `bulletFields`.

    ## Sanitization log

    No redactions required — Workday public CXS endpoint returns no candidate-identifying data.
    ```

    **jsonld/example_careers.html** — minimal HTML page with one `<script type="application/ld+json">` JobPosting block:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="utf-8">
      <title>Acme Careers — Software Engineer</title>
      <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "JobPosting",
        "title": "Software Engineer",
        "datePosted": "2026-03-15",
        "validThrough": "2026-09-15",
        "description": "<p>Build cool software at Acme.</p>",
        "employmentType": "FULL_TIME",
        "hiringOrganization": {"@type": "Organization", "name": "Acme Corp"},
        "jobLocation": {"@type": "Place", "address": {"@type": "PostalAddress", "addressLocality": "San Francisco", "addressRegion": "CA", "addressCountry": "US"}},
        "url": "https://acme.com/careers/job/100"
      }
      </script>
    </head>
    <body>
      <h1>Software Engineer at Acme</h1>
    </body>
    </html>
    ```

    All __init__.py files are EMPTY (zero bytes) — `touch` semantics. Use Write tool with empty content `""`.

    Threat note (T-04-04 mitigate): fixture files contain NO real PII. The names/IDs above are obviously-synthetic placeholders (e.g., "1ff4a4e3-aaaa-bbbb-cccc-..."). Greenhouse/airbnb existing fixture demonstrates this is the project's checked-in convention.
  </action>
  <verify>
    <automated>test -f tests/fixtures/ats/lever/spotify.json && test -f tests/fixtures/ats/ashby/ashby.json && test -f tests/fixtures/ats/smartrecruiters/visa.json && test -f tests/fixtures/ats/workday/workday_wd5.json && test -f tests/fixtures/ats/workday/workday_synthetic_wd1.json && test -f tests/fixtures/ats/workday/workday_synthetic_wd3.json && test -f tests/fixtures/ats/workday/SOURCE.md && test -f tests/fixtures/jsonld/example_careers.html && ~/.job-scout-venv/bin/python3 -c "import json; [json.loads(open(p).read()) for p in ['tests/fixtures/ats/lever/spotify.json','tests/fixtures/ats/ashby/ashby.json','tests/fixtures/ats/smartrecruiters/visa.json','tests/fixtures/ats/workday/workday_wd5.json','tests/fixtures/ats/workday/workday_synthetic_wd1.json','tests/fixtures/ats/workday/workday_synthetic_wd3.json']]; print('all fixtures parse')"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 fixture files exist (5 __init__.py + 7 JSON + 1 HTML + 1 SOURCE.md): `ls tests/fixtures/ats/lever/__init__.py tests/fixtures/ats/lever/spotify.json tests/fixtures/ats/ashby/__init__.py tests/fixtures/ats/ashby/ashby.json tests/fixtures/ats/smartrecruiters/__init__.py tests/fixtures/ats/smartrecruiters/visa.json tests/fixtures/ats/workday/__init__.py tests/fixtures/ats/workday/workday_wd5.json tests/fixtures/ats/workday/workday_synthetic_wd1.json tests/fixtures/ats/workday/workday_synthetic_wd3.json tests/fixtures/ats/workday/SOURCE.md tests/fixtures/jsonld/__init__.py tests/fixtures/jsonld/example_careers.html` exits 0
    - All __init__.py files are empty (0 bytes): `find tests/fixtures/ats/lever tests/fixtures/ats/ashby tests/fixtures/ats/smartrecruiters tests/fixtures/ats/workday tests/fixtures/jsonld -name __init__.py -size 0 | wc -l | grep -q '5'`
    - Lever fixture is a JSON ARRAY (Lever specificity): `~/.job-scout-venv/bin/python3 -c "import json; d=json.loads(open('tests/fixtures/ats/lever/spotify.json').read()); assert isinstance(d, list) and len(d) >= 3 and 'createdAt' in d[0] and 'hostedUrl' in d[0], d"` exits 0
    - Ashby fixture has at least one isListed=false: `~/.job-scout-venv/bin/python3 -c "import json; d=json.loads(open('tests/fixtures/ats/ashby/ashby.json').read()); assert any(j.get('isListed') is False for j in d['jobs']), 'no isListed=false job'"` exits 0
    - SR fixture has BOTH list and detail keys: `~/.job-scout-venv/bin/python3 -c "import json; d=json.loads(open('tests/fixtures/ats/smartrecruiters/visa.json').read()); assert 'list' in d and 'detail' in d and 'content' in d['list'] and 'jobAd' in d['detail']"` exits 0
    - Workday wd5 fixture has total + jobPostings + at least one with postedOn containing "Posted": `~/.job-scout-venv/bin/python3 -c "import json; d=json.loads(open('tests/fixtures/ats/workday/workday_wd5.json').read()); assert 'total' in d and 'jobPostings' in d and any('Posted' in j['postedOn'] for j in d['jobPostings'])"` exits 0
    - SOURCE.md contains the word "Synthetic" (D-4 documentation): `grep -c 'Synthetic' tests/fixtures/ats/workday/SOURCE.md` is at least 2
    - JSON-LD fixture has the script tag: `grep -c 'application/ld+json' tests/fixtures/jsonld/example_careers.html` returns exactly 1
    - JSON-LD fixture parses as valid JobPosting: `~/.job-scout-venv/bin/python3 -c "import re,json; html=open('tests/fixtures/jsonld/example_careers.html').read(); m=re.search(r'<script[^>]*application/ld\+json[^>]*>(.*?)</script>', html, re.DOTALL); j=json.loads(m.group(1)); assert j.get('@type')=='JobPosting' and j.get('title') and j.get('datePosted') and j.get('hiringOrganization',{}).get('name'), j"` exits 0
  </acceptance_criteria>
  <done>
    All 5 fixture directories populated; all JSON files parse; all __init__.py files exist and are empty; SOURCE.md documents D-4 (synthetic wd1/wd3); JSON-LD fixture has parseable JobPosting block.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/test_providers_phase4.py — 15 RED tests covering PRV-01..09, STR-01, STR-03</name>
  <files>tests/test_providers_phase4.py</files>
  <read_first>
    - tests/test_detection.py (analog — bootstrap pattern, MagicMock pattern, fixture loading)
    - tests/conftest.py (shared fixtures + sys.path bootstrap)
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-VALIDATION.md "Per-Task Verification Map" (15 named test functions in priority order)
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-PATTERNS.md "tests/test_providers_phase4.py" + Workday CSRF test pattern
    - .planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-RESEARCH.md (per-provider field mapping spec to assert against)
    - scripts/ats/normalize.py (Listing dataclass — required fields the tests will construct against)
  </read_first>
  <behavior>
    - 15 test functions corresponding 1:1 with VALIDATION.md's per-task verification map. Each test's docstring cites the requirement ID it covers.
    - Tests fail RED initially (provider modules don't exist; filter helpers don't exist). Wave 2 + Wave 3 plans turn them GREEN.
    - Each test uses fixture-driven assertion (load JSON file, call function, assert field values) — no live network calls.
    - Workday CSRF test uses MagicMock for httpx.Client to simulate 401 + "csrf token invalid" body and asserts FetchResult.auth_required is True.
    - Filter tests construct Listing instances directly (no fixture needed) and assert filter_stale / collapse_regional_dupes / filter_evergreen behavior.
    - Registry test asserts `len(PROVIDERS) == 6` (greenhouse + lever + ashby + smartrecruiters + workday + jsonld) AFTER Wave 3 wires it; passing now would mean PROVIDERS already has 6 entries which is wrong — this is intentionally RED until Plan 04-05.
  </behavior>
  <action>
    Delegate the test-file generation to cf-code-assistant MCP per project routing rules. Pass:
    - The full 15-test spec from VALIDATION.md "Per-Task Verification Map"
    - The bootstrap + MagicMock + fixture-loading patterns extracted verbatim from tests/test_detection.py + 04-PATTERNS.md
    - The Listing dataclass shape (required fields from scripts/ats/normalize.py)
    - The Workday CSRF detection contract (status=401 + body containing "csrf"/"session"/"cookie"/"authentication" → auth_required=True)
    - The filter contract from 04-PATTERNS.md `apply_filters` section

    The 15 test functions (matching VALIDATION.md exactly):

    1. **test_lever_to_listing** (PRV-01) — `from ats.providers import lever`; load `LEVER_FIXTURE = json.loads(...spotify.json)`; call `lever.to_listing(LEVER_FIXTURE[0])`; assert title=="Senior Software Engineer", url starts with "https://jobs.lever.co/spotify/", source=="ats:lever", posted_date matches `^\d{4}-\d{2}-\d{2}$`, location=="Remote - USA".
    2. **test_lever_posted_date_epoch** (PRV-01) — Lever's `createdAt: 1773335421350` (epoch ms) → posted_date is a non-empty ISO date (not the literal epoch number); assert `len(posted_date) == 10` and `posted_date[:4] == "2026"` (epoch 1773335421350 ms = 2026 date).
    3. **test_ashby_filters_unlisted** (PRV-02) — Use a MagicMock httpx.Client returning the ashby fixture; assert `ashby.fetch("ashby", client, MagicMock())` returns FetchResult with len(listings) == 2 (the two `isListed: true` jobs), the `isListed: false` "Internal Draft Role" is NOT in listings.
    4. **test_ashby_to_listing** (PRV-02) — Load fixture, call `ashby.to_listing(fixture['jobs'][0])`; assert source=="ats:ashby", posted_date=="2026-03-15", employment_type=="Full-time" (FullTime → Full-time normalization), title=="Software Engineer".
    5. **test_sr_to_listing** (PRV-03) — Load `SR_FIXTURE['detail']`; call `smartrecruiters.to_listing(detail)`; assert source=="ats:smartrecruiters", title=="Product Manager" (from `name`), company=="Visa" (from `company.name`), posted_date=="2026-03-15", description contains "cross-border payments" (HTML-stripped).
    6. **test_sr_description_from_detail** (PRV-03) — Use a MagicMock client where `client.get` is called twice (list URL → detail URL); fixture `list` returns `list_resp.json()` and `detail` returns `detail_resp.json()`. Mock `client.get.side_effect = [list_resp_mock, detail_resp_mock]`. Call `smartrecruiters.fetch("Visa", client, MagicMock_sem)`; assert listings[0].description contains the detail's HTML-stripped text.
    7. **test_workday_to_listing** (PRV-04) — Load workday_wd5 fixture; call `workday.to_listing(fixture['jobPostings'][0], tenant="workday", dc="wd5", site="Workday")`; assert source=="ats:workday", title=="Account Executive", location=="Osaka, Japan", url contains "/en-US/Workday/job/JPNOsaka/", posted_date matches ISO date.
    8. **test_workday_posted_on_parsing** (PRV-04) — Test `_parse_workday_posted_on` (or whatever the helper is named): "Posted Today" → today's ISO date; "Posted 5 Days Ago" → today - 5d; "Posted 30+ Days Ago" → today - 30d; "" → "" (empty input). Use `today=date(2026, 4, 28)` parameter for determinism.
    9. **test_workday_csrf_detection** (PRV-05) — MagicMock client, mock_resp.status_code=401, mock_resp.text="csrf token invalid"; call `workday.fetch("https://workday.wd5.myworkdayjobs.com/Workday", client, sem_mock)`; assert result.auth_required is True, result.listings == [], result.http_status == 401.
    10. **test_filter_stale** (PRV-06) — Construct two Listings (one with posted_date 4 months old, one 10 days old); call `filter_stale([old, fresh], max_age_days=60, today=date(2026,4,28))`; assert fresh in result, old not in result.
    11. **test_filter_stale_per_provider_override** (STR-03) — Construct Listing with posted_date 75 days old, source="ats:workday"; call `filter_stale([listing], max_age_days=60, provider_name="workday", provider_overrides={"workday": 90}, today=date(2026,4,28))`; assert listing IS in result (90d override beats 60d default).
    12. **test_collapse_regional_dupes** (PRV-07) — Construct three Listings with same source/company/title but different locations ("New York, NY", "London, UK", "Tokyo, Japan"); call `collapse_regional_dupes(listings)`; assert len(result) == 1, result[0].location contains all three locations joined OR is "Multiple Locations" if logic differs (read normalize.py contract from 04-PATTERNS.md to confirm exact fallback string).
    13. **test_filter_evergreen** (PRV-08) — Construct Listings with titles "Talent Network — Future Opportunities", "General Application", "Senior Software Engineer", "Join Our Team"; call `filter_evergreen(listings)`; assert ONLY "Senior Software Engineer" remains.
    14. **test_providers_registry_has_five** (PRV-09) — Despite the name, this asserts `len(PROVIDERS) == 6` after Phase 4 (5 real ATS + JSON-LD per D-3). `from ats import PROVIDERS; assert "lever" in PROVIDERS and "ashby" in PROVIDERS and "smartrecruiters" in PROVIDERS and "workday" in PROVIDERS and "jsonld" in PROVIDERS and "greenhouse" in PROVIDERS; assert len(PROVIDERS) == 6`.
    15. **test_jsonld_extraction** (STR-01) — Load `tests/fixtures/jsonld/example_careers.html` as text; call `jsonld._extract_jsonld_jobs(html_content)` (or whatever the public extraction function is — confirm name matches spec in 04-PATTERNS.md); assert len(jobs) == 1, jobs[0].get("@type") == "JobPosting", jobs[0].get("title") == "Software Engineer".

    Bootstrap (verbatim from test_detection.py lines 29-38):
    ```python
    """test_providers_phase4.py — Wave 0 RED tests for PRV-01..09, STR-01, STR-03.

    All 15 tests are expected to FAIL until Wave 2 (provider modules) and
    Wave 3 (filters + registry wiring) plans land. Run with:

        ~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py -x -q
    """
    import json
    import sys
    from datetime import date, timedelta
    from pathlib import Path
    from unittest.mock import MagicMock

    import pytest

    PROJECT_ROOT = Path(__file__).resolve().parent.parent
    SCRIPTS_DIR = PROJECT_ROOT / "scripts"
    sys.path.insert(0, str(SCRIPTS_DIR))

    LEVER_FIXTURE = PROJECT_ROOT / "tests" / "fixtures" / "ats" / "lever" / "spotify.json"
    ASHBY_FIXTURE = PROJECT_ROOT / "tests" / "fixtures" / "ats" / "ashby" / "ashby.json"
    SR_FIXTURE = PROJECT_ROOT / "tests" / "fixtures" / "ats" / "smartrecruiters" / "visa.json"
    WD_WD5_FIXTURE = PROJECT_ROOT / "tests" / "fixtures" / "ats" / "workday" / "workday_wd5.json"
    JSONLD_FIXTURE = PROJECT_ROOT / "tests" / "fixtures" / "jsonld" / "example_careers.html"
    ```

    Each test imports the relevant module INSIDE the function body (so import failures show up per-test rather than killing the whole module). Example:

    ```python
    def test_lever_to_listing():
        """PRV-01: lever.to_listing() maps fixture fields to Listing."""
        from ats.providers import lever  # RED until Plan 04-02 lands
        payload = json.loads(LEVER_FIXTURE.read_text())
        listing = lever.to_listing(payload[0])
        assert listing.title == "Senior Software Engineer"
        assert listing.source == "ats:lever"
        assert listing.url.startswith("https://jobs.lever.co/spotify/")
        assert listing.location == "Remote - USA"
        # Lever createdAt is epoch ms; to_listing converts to ISO date
        assert len(listing.posted_date) == 10
        assert listing.posted_date.startswith("2026-")
    ```

    Use cf-code-assistant via the mcp__cf-code-assistant__generateCode tool. Pass the test spec, bootstrap pattern, and Listing/MagicMock contract as `context` per the project's never-call-cold rule.

    Then run the test file and confirm ALL 15 tests are RED (collection succeeds, but each test fails or errors). This is the desired Wave 0 state.
  </action>
  <verify>
    <automated>~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py --collect-only -q 2>&1 | grep -c "test_" | awk '{if ($1 >= 15) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_providers_phase4.py`
    - File has at least 15 test functions: `grep -c '^def test_' tests/test_providers_phase4.py` returns at least `15`
    - All 15 named tests present (collection check): `~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py --collect-only -q 2>&1 | grep -E "test_(lever_to_listing|lever_posted_date_epoch|ashby_filters_unlisted|ashby_to_listing|sr_to_listing|sr_description_from_detail|workday_to_listing|workday_posted_on_parsing|workday_csrf_detection|filter_stale|filter_stale_per_provider_override|collapse_regional_dupes|filter_evergreen|providers_registry_has_five|jsonld_extraction)" | wc -l | awk '{if ($1 >= 15) exit 0; else exit 1}'`
    - All 15 tests are RED (intentionally — Wave 2/3 will turn them GREEN): `~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py -q 2>&1 | tail -1 | grep -E "(failed|error)"` exits 0 (i.e. there are failures/errors, not all-passes)
    - Existing test suite still passes: `~/.job-scout-venv/bin/python3 -m pytest tests/test_migration.py tests/test_detection.py -x -q` exits 0
    - Bootstrap pattern matches existing convention: `grep -c "PROJECT_ROOT = Path(__file__).resolve().parent.parent" tests/test_providers_phase4.py` returns at least 1
    - Each Phase 4 requirement ID appears in at least one test docstring: `grep -c "PRV-01\|PRV-02\|PRV-03\|PRV-04\|PRV-05\|PRV-06\|PRV-07\|PRV-08\|PRV-09\|STR-01\|STR-03" tests/test_providers_phase4.py` returns at least 11
  </acceptance_criteria>
  <done>
    tests/test_providers_phase4.py exists with at least 15 test functions; pytest collects all 15; all 15 currently fail (RED); existing Phase 1/3 tests still pass; each Phase 4 requirement has at least one test docstring naming it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Provider modules (Wave 2) | Untrusted JSON shapes loaded from `tests/fixtures/ats/*/`; per-job ValueError on missing required Listing field is the existing safe-failure path. New fixtures must NOT contain real PII. |
| `detect.py` PROVIDERS-iteration → empty BOARD_URL_PATTERNS | New skip-guard is a TRUST BOUNDARY: it lets jsonld.py opt out of detection probes. Misuse (e.g., a future provider with empty patterns by accident) silently disables detection for that provider. Mitigated by D-3 inline comment + acceptance criterion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tests/fixtures/ats/*/* | mitigate | Fixtures use synthetic IDs (e.g., `1ff4a4e3-aaaa-bbbb-cccc-...`) and obviously-fake names. Acceptance criterion ensures NO real candidate PII captured. SOURCE.md documents sanitization. |
| T-04-02 | Tampering | scripts/ats/providers/base.py FetchResult | mitigate | New field is additive with default `False`; frozen dataclass prevents post-construction mutation; existing call sites unchanged so no breaking-change vector. |
| T-04-03 | Denial of Service | scripts/ats/detect.py PROVIDERS loop | accept | The new skip guard reduces network calls (skips JSON-LD probing); strictly improves DoS posture. No mitigation needed. |
| T-04-04 | Spoofing | tests/fixtures/jsonld/example_careers.html | mitigate | Fixture clearly identifies "Acme Corp" / "acme.com" — recognizable placeholder, no real careers page imitated. |
| T-04-05 | Repudiation | SOURCE.md provenance for Workday | mitigate | SOURCE.md explicitly tags wd1/wd3 as SYNTHETIC per D-4; future maintainer reading runs.jsonl can match outcomes against fixture provenance and know which were live-validated vs synthetic. |
</threat_model>

<verification>
After all 3 tasks complete:

1. **base.py + detect.py change is additive + non-breaking:**
   ```bash
   grep -c 'auth_required: bool = False' scripts/ats/providers/base.py  # → 1
   grep -c 'if not getattr(provider, "BOARD_URL_PATTERNS", None):' scripts/ats/detect.py  # → 1
   ~/.job-scout-venv/bin/python3 -m pytest tests/test_migration.py tests/test_detection.py -x -q  # → all green
   ```

2. **All 5 fixture sets present + parseable:**
   ```bash
   find tests/fixtures/ats/lever tests/fixtures/ats/ashby tests/fixtures/ats/smartrecruiters tests/fixtures/ats/workday tests/fixtures/jsonld -type f | wc -l  # → at least 13
   ~/.job-scout-venv/bin/python3 -c "import json; [json.loads(open(p).read()) for p in ['tests/fixtures/ats/lever/spotify.json','tests/fixtures/ats/ashby/ashby.json','tests/fixtures/ats/smartrecruiters/visa.json','tests/fixtures/ats/workday/workday_wd5.json','tests/fixtures/ats/workday/workday_synthetic_wd1.json','tests/fixtures/ats/workday/workday_synthetic_wd3.json']]; print('OK')"
   ```

3. **15 RED tests in place:**
   ```bash
   ~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py --collect-only -q 2>&1 | grep -c "test_" | awk '{print "tests collected: "$1}'  # → ≥15
   ~/.job-scout-venv/bin/python3 -m pytest tests/test_providers_phase4.py -q 2>&1 | tail -1  # → contains "failed" or "error" (not all green)
   ```

4. **No regressions in existing test suite:**
   ```bash
   ~/.job-scout-venv/bin/python3 -m pytest tests/test_migration.py tests/test_detection.py -q  # → all green
   ```
</verification>

<success_criteria>
1. **D-1 landed:** `auth_required: bool = False` in FetchResult; greenhouse.py + dispatcher.py untouched (verified via test_migration + test_detection passing).
2. **D-3 landed:** `if not getattr(provider, "BOARD_URL_PATTERNS", None): continue` guard in detect.py PROVIDERS-iteration loop, before the semaphore acquisition.
3. **D-4 landed:** workday/SOURCE.md documents wd5 (live) vs wd1+wd3 (synthetic).
4. **Wave 0 complete:** 15 RED tests in tests/test_providers_phase4.py; 5 fixture trees in place; existing Phase 1+3 test suite unaffected.
5. **Wave 2 unblocked:** Plans 04-02, 04-03, 04-04 can each be implemented independently against this scaffolding (no shared file conflicts, no contract revisions needed).
</success_criteria>

<output>
After completion, create `.planning/phases/04-remaining-providers-lever-ashby-smartrecruiters-workday-json-ld-fallback-filtering-layer/04-01-scaffolding-SUMMARY.md`
</output>
